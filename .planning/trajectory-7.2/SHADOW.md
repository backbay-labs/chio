# Chio 7.2 Shadow

The 7.2 shadow target is production runtime orchestration after local verifier-owned runtime policy is proven.

Candidate scope:

- configure runtime policy providers for long-running local kernels
- extend local loopback into production-ready orchestration controls
- preserve verifier-owned trust roots and fail-closed admission
- keep dynamic trust, peer discovery, settlement, hidden predicates, VC Data Integrity BBS, zkVM, FROST, and new transports deferred unless explicitly promoted
