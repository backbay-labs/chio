# Owners

- Integrator: branch, planning, docs, gates, and closeout.
- Runtime contracts: `chio-runtime-core` schemas, types, validators, and
  stable failure codes.
- Runtime store: SQLite orchestration/admission state and restart behavior.
- CLI: `chio runtime orchestrate` commands and fixture wiring.
- Assurance: runtime orchestration gate, negative corpus, and CI trigger.
