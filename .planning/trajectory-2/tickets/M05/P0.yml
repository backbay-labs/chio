# M05 Phase 0 - Wave-opener Cargo.lock bump.
# Source of truth: .planning/trajectory-2/05-adversarial-escape-threat-model.md
# (Phase 0). Pins direct deps required downstream so the load-bearing P1 / P3
# / P4 / P5 phases do not contend with `Cargo.lock` churn.
#
# Wave: W2 (trust-boundary regression nets), parallel with M03 and M04.
# Trust-boundary milestone: every M05 PR carries the M05 freeze label.
#
# Regenerate the manifest after edits:
# `cargo xtask trajectory regen-manifest` (post-trajectory-2 manifest tooling).

- id: M05.P0.T1
  milestone: M05
  phase: 0
  title: Pin toml direct dep on chio-attest-verify and arbitrary on chio-wasm-guards tests
  status: merged
  effort_days: 0.5
  owner_glob:
    - crates/chio-attest-verify/Cargo.toml
    - crates/chio-wasm-guards/Cargo.toml
  shared_paths:
    - Cargo.lock
  depends_on: []
  soft_deps:
    - "trajectory-1 M02.P4.T2 (scripts/promote_fuzz_seed.sh) is the script M05.P2.T1 extends; lock churn must not collide"
  gate_check:
    cmd: cargo build -p chio-attest-verify --quiet && cargo build -p chio-wasm-guards --quiet && cargo tree -p chio-attest-verify --depth 1 | grep -q '^[├└]── toml '
    timeout_seconds: 600
  worktree_branch: wave/W2/m05/p0.t1-cargo-lock-bump-toml-arbitrary-v2
  agent_role: quality-rust
  review_required:
    - "@bb-connor"
  first_commit_sha: 9c622022bf4197e38f12e56bac72313bc9540a6a
  merged_sha: e8fe0d2b47dc40ca8fc1c7eeab4cd170b7183fff
  opened_ts: "2026-04-30T15:33:39Z"
  merged_ts: "2026-04-30T15:43:42Z"
