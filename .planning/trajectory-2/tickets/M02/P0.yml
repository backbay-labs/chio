# M02 Phase 0 - Wave-opener Cargo.lock bump and audit doc.
# Source of truth: .planning/trajectory-2/02-mutation-and-cross-sdk-differential.md
# (Phase P0 section, "Wave-opener Cargo.lock bump and audit doc"). Wave 1.
#
# Milestone scope: M02 lands in Wave 1 in parallel with M01 and M06 on
# `wave/W1/m02/...` worktrees. P0 -> P1 -> P2 -> P3 are the mutation half;
# P4 -> P5 are the verdict-matrix half. P4 may start as soon as P0 is green
# (it does not depend on the mutation baselines). P5 starts after P4.
#
# CROSS-MILESTONE NOTE: The reason-code taxonomy in the verdict-matrix
# diff oracle reuses the M01 (trajectory-2) `urn:chio:error:*` registry.
# That dependency is encoded as a soft_dep string sentence on M02.P4.T4
# (diff oracle); the orchestrator gates the ticket on M01.P1.T1 merged_sha
# via the wave-1 sync rule, not via a trajectory-2 depends_on edge.
#
# Regenerate the manifest after edits: `cargo xtask trajectory regen-manifest`.

- id: M02.P0.T1
  milestone: M02
  phase: 0
  title: Open M02 audit doc and snapshot trajectory-1 M02.P3 advisory state
  status: review
  effort_days: 0.5
  owner_glob:
    - .planning/audits/M02-mutation-and-verdict-matrix.md
  shared_paths: []
  depends_on: []
  soft_deps:
    - "trajectory-1 M02.P3 staged .cargo/mutants.toml and .github/workflows/mutants.yml as advisory shells; this milestone promotes them"
  gate_check:
    cmd: test -f .planning/audits/M02-mutation-and-verdict-matrix.md && grep -q 'trajectory-1 M02.P3 advisory state' .planning/audits/M02-mutation-and-verdict-matrix.md
    timeout_seconds: 60
  worktree_branch: wave/W1/m02/p0.t1-open-audit-doc-and-snapshot-baseline
  agent_role: quality-rust
  review_required:
    - "@bb-connor"
  first_commit_sha: 998a2babded9a8535eb1735b808ada24839452aa
  merged_sha: null
  opened_ts: "2026-04-29T18:14:23.386912Z"
  merged_ts: null

- id: M02.P0.T2
  milestone: M02
  phase: 0
  title: Cargo.lock bump and cargo-mutants 25.x re-pin verification
  status: pending
  effort_days: 0.5
  owner_glob:
    - .github/workflows/mutants.yml
  shared_paths:
    - Cargo.lock
  depends_on:
    - M02.P0.T1
  soft_deps: []
  gate_check:
    cmd: grep -q "cargo-mutants --version '~25'" .github/workflows/mutants.yml && cargo build --workspace --quiet
    timeout_seconds: 600
  worktree_branch: wave/W1/m02/p0.t2-cargo-lock-bump-and-mutants-pin
  agent_role: quality-rust
  review_required:
    - "@bb-connor"
  first_commit_sha: null
  merged_sha: null
  opened_ts: null
  merged_ts: null
