# Bar 1 baseline -- mutation kill banner + threat-evidence directory

**Bar**: 1 (Lane A: realize the floor).
**Baseline captured**: 2026-05-08.
**Baseline SHA**: `708c7bb33df43594f5e76542b05fca7a56d9689e`.
**Baseline branch**: `claude/charming-feistel-8595da` (parent of TRJ5-A1 lane PRs).
**Authoritative source**: `README.md` workspace mutation banner +
`audits/evidence/threats/*.json` files +
`audits/evidence/mutants/<crate>/2026-05-08.json` per-crate runs.

## Coordinated across PRs

This file is coordinated across the TRJ5-A1 mutation-baseline lane
PRs: #603 (chio-credentials), #619 (chio-attest-verify),
#621 (chio-guards), #622 (chio-anchor), #623 (chio-policy), and the
chio-weights and chio-kernel-core PRs that follow. Whichever PR lands
last on `main` inherits trivial conflicts on the per-crate table; all
PRs in the lane carry the same content shape.

This particular PR (chio-weights) seeds the file on `main` because no
prior TRJ5-A1 PR has merged to `main` yet. The PRs that pre-date this
seed (#603, #619, #621, #622, #623) carry their own copy of this
file with their respective rows; merging them into `main` after this
PR lands will produce a single coordinated table.

---

## Mutation banner (current, pre-trj5)

From `README.md` line 17 (verbatim, captured 2026-05-08):

> "Mutation kill: 31% - six-crate trust-boundary mutation baseline,
>  mixed sweep/shard n=375 viable mutants - 2026-04-29"

| Field | Value |
|---|---|
| Workspace mutation kill (banner) | 31% |
| Banner measurement date | 2026-04-29 |
| Sample size | n=375 viable mutants |
| Crate scope | 6 trust-boundary crates (banner does not enumerate them) |
| Per-crate breakdown | PARTIAL -- 1 of 6+1 measured (chio-weights supplementary). See coordinated table below. |

### Measured per-crate baseline (work-in-progress)

The PR continuing the TRJ5-A1 lane lands per-crate measurements
incrementally. This file's per-crate table reflects the rows that
have landed on `main` so far; rows for crates whose TRJ5-A1 PRs are
open but unmerged remain `BASELINE-GAP`.

Current per-crate state (regenerated by
`bash audits/mutation/aggregate.sh chio-policy chio-credentials
chio-attest-verify chio-kernel-core chio-guards chio-anchor
chio-weights`):

<!-- COORDINATED ACROSS PRs (chio-weights seeds the file on main) -->
<!-- AUTOGENERATED -->
| Crate | Total | Caught | Missed | Timeout | Unviable | Kill rate / status |
|---|---|---|---|---|---|---|
| `chio-policy` | BASELINE-GAP | - | - | - | - | **n/a** (PR #623 open) |
| `chio-credentials` | BASELINE-GAP | - | - | - | - | **n/a** (PR #603 open) |
| `chio-attest-verify` | BASELINE-GAP | - | - | - | - | **n/a** (PR #619 open) |
| `chio-kernel-core` | BASELINE-GAP | - | - | - | - | **n/a** (PR open in parallel) |
| `chio-guards` | BASELINE-GAP | - | - | - | - | **n/a** (PR #621 open) |
| `chio-anchor` | BASELINE-GAP | - | - | - | - | **n/a** (PR #622 open) |
| `chio-weights` | 66 of 66 (FULL) | 43 | 20 | 0 | 3 | **FULL -- 68.3% on 66/66** (this PR; target met for >=65%) |
<!-- END -->

**Crate-level target satisfaction**:

- `chio-weights` (68.3%) clears the `>=65%` target on a FULL 66/66
  mutant run. The examine_globs surface covers all four logic-bearing
  source files (`bundle.rs`, `card.rs`, `error.rs`, `lineage.rs`).
  Note: chio-weights is not in the canonical six trust-boundary
  crates per `releases.toml [trust_boundary_crates]`; it is treated
  as trust-boundary for TRJ5-A1 purposes because the model-card
  binding refusal is the kernel's last line of defence against a
  malformed-card or scope-overreach provider bind.
- All six canonical trust-boundary crates remain BASELINE-GAP on
  `main` until the corresponding TRJ5-A1 PRs land. The PRs that
  pre-date this seed (#603, #619, #621, #622, #623) and the
  chio-kernel-core PR (parallel) each measure one row of the
  canonical six.

The chio-attest-verify number is the load-bearing one for Bar 1
because the trj5 contract is `>= 80%` on this crate (per
`lane-a-floor/mutation-budget.md` line 22). chio-weights is on the
non-load-bearing `>= 65%` track; the >=65% target met here does not
substitute for any canonical-six measurement.

## Mutation evidence directory (current, pre-trj5)

`audits/evidence/mutants/` exists post-this-seed-PR with one row:
`audits/evidence/mutants/chio-weights/`. Each per-crate directory
contains a `<date>.json` per-crate summary and a `mutants.out/` with
the full cargo-mutants log; rows for the canonical six land via the
parallel TRJ5-A1 PRs.

## Threat-evidence directory (current, pre-trj5)

The 20 threat-evidence placeholders under `audits/evidence/threats/`
are unchanged by this PR; threat-row triage is the TRJ5-A2 lane's
deliverable (PRs in flight under `claude/trj5/a2-threat-evidence-*`).

## Post-#613 rerun note

PR #613 (chio-weights Kani harness) is open against main and not yet
merged. The chio-weights row above is measured against current main
(`708c7bb33`) without #613's harness. Once #613 lands, the Kani
harness exercises additional invariants (notably the kernel binding
refusal contract) and a re-run is expected to score higher than
68.25% on the same examine_globs surface. The CI hosted-nightly
mutants.yml lane (4-hour-per-crate budget) is the authoritative
re-baseline after #613 merges.
