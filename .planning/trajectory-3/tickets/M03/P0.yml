# M03 Phase 0 - Audit baseline + billing-restoration plan + rebuilder pre-name.
# Source of truth: .planning/trajectory-3/03-hosted-ci-truth-and-reproducible-builds.md
# (section "Phases" P0).
#
# Milestone scope (from .planning/trajectory-3/03-...md): P0 lands the audit
# doc with reproducible hard counts, formalises the billing-restoration
# runbook so the GitHub Actions account owner can act in under 10 minutes,
# pre-names the third-party rebuilder per D13 so lead time does not
# bottleneck P5, and documents the reproducibility-scope carve-out
# (Linux x86_64 only for v3.18; macOS / Windows out of scope).
#
# CROSS-MILESTONE GATE: P0 is the wave-opener for M03. Every M03 phase
# below depends transitively on P0.T1 (the audit doc). M03 in turn
# unblocks every other trajectory-3 milestone via the hosted-CI lane;
# see section "Cross-milestone interactions" of the milestone narrative.
#
# M03 IS a trust-boundary milestone (release legibility); reviewer is
# the human-side @bb-connor only on a single-owner trajectory.
#
# Regenerate the manifest after edits: `cargo xtask trajectory regen-manifest`.

- id: M03.P0.T1
  milestone: M03
  phase: 0
  title: Open audit doc M03-ci-restoration with hard counts (4 days since bypass; 0 YAML-disabled but 41 account-blocked; 118 PRs in #306-#425)
  status: pending
  effort_days: 0.5
  owner_glob:
    - .planning/trajectory-3/audits/M03-ci-restoration.md
  shared_paths: []
  depends_on: []
  soft_deps:
    - "trajectory-2 admin-merge bypass began 2026-04-26T23:00:42Z; PR range #306-#425 totals 118 merged PRs; sample of last 500 workflow runs returns 489 failure / 10 cancelled / 1 success"
    - "research findings live at .planning/trajectory-3/research/m03/RESEARCH.md sections 'GitHub Actions billing failure mode' and 'Admin-merge bypass inventory and bisect plan'"
  gate_check:
    cmd: "test -f .planning/trajectory-3/audits/M03-ci-restoration.md && grep -q '118' .planning/trajectory-3/audits/M03-ci-restoration.md && grep -q '2026-04-26T23:00' .planning/trajectory-3/audits/M03-ci-restoration.md && grep -qE '6,?865' .planning/trajectory-3/audits/M03-ci-restoration.md && grep -q 'Linux x86_64' .planning/trajectory-3/audits/M03-ci-restoration.md"
    timeout_seconds: 600
  worktree_branch: wave/W1/m03/p0.t1-audit-doc-baseline-counts
  agent_role: infra-yaml
  review_required:
    - "@bb-connor"
  first_commit_sha: null
  merged_sha: null
  opened_ts: null
  merged_ts: null

- id: M03.P0.T2
  milestone: M03
  phase: 0
  title: Author docs/runbooks/ci-billing.md formalising the GitHub Actions billing-restoration runbook
  status: pending
  effort_days: 0.5
  owner_glob:
    - docs/runbooks/ci-billing.md
  shared_paths: []
  depends_on:
    - M03.P0.T1
  soft_deps:
    - "Per-run cost estimate (cold cache `Build, lint, test` 30 min ubuntu-latest = $0.24; `Coverage` up to 180 min = $1.44; `MSRV` 15 min = $0.12; `cargo-vet` + `cargo-deny` 10 min combined = $0.16; `formal-tla` + `kani-public-pr` up to $0.72) implies $3-$5 per PR full sweep and $30-$50/day at trajectory-3 cadence; cap floor must accommodate plus 50% headroom"
    - "billing-account ownership is currently personal (github.com/bb-connor/arc); org migration is a separate trajectory and out of M03 scope"
  gate_check:
    cmd: "test -f docs/runbooks/ci-billing.md && grep -q 'spending limit' docs/runbooks/ci-billing.md && grep -q 'gh workflow run' docs/runbooks/ci-billing.md && grep -qE '\\$[0-9]+' docs/runbooks/ci-billing.md"
    timeout_seconds: 600
  worktree_branch: wave/W1/m03/p0.t2-billing-restoration-runbook
  agent_role: infra-yaml
  review_required:
    - "@bb-connor"
  first_commit_sha: null
  merged_sha: null
  opened_ts: null
  merged_ts: null

- id: M03.P0.T3
  milestone: M03
  phase: 0
  title: Pre-name third-party rebuilder per D13 (primary plus backup) in audit doc section 3
  status: pending
  effort_days: 0.5
  owner_glob:
    - .planning/trajectory-3/audits/M03-ci-restoration.md
  shared_paths: []
  depends_on:
    - M03.P0.T1
  soft_deps:
    - "D13 names independent contributor or sister team; recommendation per RESEARCH.md is a sister-team engineer outside the chio core team"
    - "if M08 lands NCC Group / Trail of Bits, fold the rebuild into their engagement scope as higher-weight evidence; M03 records the optionality but does not block on M08"
  gate_check:
    cmd: "grep -A2 -E 'rebuilder identity' .planning/trajectory-3/audits/M03-ci-restoration.md | grep -qiE 'primary|backup'"
    timeout_seconds: 300
  worktree_branch: wave/W1/m03/p0.t3-name-third-party-rebuilder
  agent_role: vendor-coord
  review_required:
    - "@bb-connor"
  first_commit_sha: null
  merged_sha: null
  opened_ts: null
  merged_ts: null

- id: M03.P0.T4
  milestone: M03
  phase: 0
  title: Document reproducibility scope carve-out in audit doc (Linux x86_64 yes; macOS and Windows out of scope for v3.18)
  status: pending
  effort_days: 0.25
  owner_glob:
    - .planning/trajectory-3/audits/M03-ci-restoration.md
  shared_paths: []
  depends_on:
    - M03.P0.T1
  soft_deps:
    - "macOS reproducibility blocked by codesign timestamps; Windows reproducibility blocked by PE COFF timestamps; both are out of scope per RESEARCH.md section 'Reproducible-build readiness'"
  gate_check:
    cmd: "grep -q 'Linux x86_64' .planning/trajectory-3/audits/M03-ci-restoration.md && grep -qiE 'macOS|windows' .planning/trajectory-3/audits/M03-ci-restoration.md && grep -qiE 'out of scope|carve.?out' .planning/trajectory-3/audits/M03-ci-restoration.md"
    timeout_seconds: 300
  worktree_branch: wave/W1/m03/p0.t4-repro-scope-carveout
  agent_role: infra-yaml
  review_required:
    - "@bb-connor"
  first_commit_sha: null
  merged_sha: null
  opened_ts: null
  merged_ts: null
