name: Chiodos Pheromone Runtime

on:
  pull_request:
    paths:
      - "crates/chio-pheromone-runtime/**"
      - "crates/chio-pheromone/**"
      - "crates/chio-federation/**"
      - "crates/chio-cli/**"
      - "examples/chiodos-3vendor/**"
      - "spec/schemas/chio-pheromone/**"
      - "scripts/check-chiodos-pheromone-runtime.sh"
      - ".github/workflows/chiodos-pheromone-runtime.yml"
  push:
    branches: [main]
    paths:
      - "crates/chio-pheromone-runtime/**"
      - "crates/chio-pheromone/**"
      - "crates/chio-federation/**"
      - "crates/chio-cli/**"
      - "examples/chiodos-3vendor/**"
      - "spec/schemas/chio-pheromone/**"
      - "scripts/check-chiodos-pheromone-runtime.sh"
      - ".github/workflows/chiodos-pheromone-runtime.yml"

jobs:
  chiodos-pheromone-runtime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Check Chiodos pheromone runtime
        run: bash scripts/check-chiodos-pheromone-runtime.sh
