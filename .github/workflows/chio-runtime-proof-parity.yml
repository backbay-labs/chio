name: Chio Runtime Proof Parity

on:
  pull_request:
    paths:
      - "crates/chio-attest-buyer-core/**"
      - "crates/chio-runtime-core/**"
      - "crates/chio-runtime-harness/**"
      - "crates/chio-attest-loopback/**"
      - "crates/chio-cli/**"
      - "crates/chio-kernel/**"
      - "crates/chio-runtime/**"
      - "examples/chio-3vendor/**"
      - "scripts/check-chio-runtime-proof-parity.sh"
      - "scripts/check-chio-runtime-spine.sh"
      - "scripts/check-chio-runtime-spine-fixtures.sh"
      - "scripts/check-chio-treaty-buyer-hero-loop.sh"
      - "spec/schemas/chio-attest/**"
      - "spec/schemas/chio-federation/**"
      - "spec/schemas/chio-runtime/**"
      - "spec/schemas/chio/**"
      - "spec/schemas/registry.json"
      - "spec/schemas/MANIFEST.sha256"
      - ".github/workflows/chio-runtime-proof-parity.yml"
  push:
    branches: ["main"]
    paths:
      - "crates/chio-attest-buyer-core/**"
      - "crates/chio-runtime-core/**"
      - "crates/chio-runtime-harness/**"
      - "crates/chio-attest-loopback/**"
      - "crates/chio-cli/**"
      - "crates/chio-kernel/**"
      - "crates/chio-runtime/**"
      - "examples/chio-3vendor/**"
      - "scripts/check-chio-runtime-proof-parity.sh"
      - "scripts/check-chio-runtime-spine.sh"
      - "scripts/check-chio-runtime-spine-fixtures.sh"
      - "scripts/check-chio-treaty-buyer-hero-loop.sh"
      - "spec/schemas/chio-attest/**"
      - "spec/schemas/chio-federation/**"
      - "spec/schemas/chio-runtime/**"
      - "spec/schemas/chio/**"
      - "spec/schemas/registry.json"
      - "spec/schemas/MANIFEST.sha256"
      - ".github/workflows/chio-runtime-proof-parity.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: chio-runtime-proof-parity-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  runtime-proof-parity:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: bash scripts/check-chio-runtime-proof-parity.sh
