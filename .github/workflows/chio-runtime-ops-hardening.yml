name: Chio Runtime Ops Hardening

on:
  push:
    branches: [main]
    paths:
      - "crates/chio-runtime-core/**"
      - "crates/chio-cli/**"
      - "crates/chio-kernel/**"
      - "crates/chio-runtime/**"
      - "examples/chio-3vendor/**"
      - "spec/schemas/chio-runtime/**"
      - "spec/schemas/**"
      - "spec/schemas/registry.json"
      - "spec/schemas/MANIFEST.sha256"
      - "scripts/check-chio-runtime-ops-hardening.sh"
      - ".github/workflows/chio-runtime-ops-hardening.yml"
  pull_request:
    paths:
      - "crates/chio-runtime-core/**"
      - "crates/chio-cli/**"
      - "crates/chio-kernel/**"
      - "crates/chio-runtime/**"
      - "examples/chio-3vendor/**"
      - "spec/schemas/chio-runtime/**"
      - "spec/schemas/**"
      - "spec/schemas/registry.json"
      - "spec/schemas/MANIFEST.sha256"
      - "scripts/check-chio-runtime-ops-hardening.sh"
      - ".github/workflows/chio-runtime-ops-hardening.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: chio-runtime-ops-hardening-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  runtime-ops-hardening:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Chio runtime ops gate
        run: bash scripts/check-chio-runtime-ops-hardening.sh
