name: Chiodos Pheromone Relay Alert Assurance Archive Hardening

on:
  pull_request:
    paths:
      - "crates/chio-pheromone-relay/**"
      - "crates/chio-cli/**"
      - "crates/chio-spec-validate/**"
      - "examples/chiodos-3vendor/fixtures/pheromone/relay/**"
      - "spec/schemas/chio-pheromone/v1/**"
      - "spec/schemas/registry.json"
      - "scripts/check-chiodos-pheromone-relay-alert-assurance-archive-hardening.sh"
      - ".github/workflows/chiodos-pheromone-relay-alert-assurance-archive-hardening.yml"
  push:
    branches: [main]
    paths:
      - "crates/chio-pheromone-relay/**"
      - "crates/chio-cli/**"
      - "crates/chio-spec-validate/**"
      - "examples/chiodos-3vendor/fixtures/pheromone/relay/**"
      - "spec/schemas/chio-pheromone/v1/**"
      - "spec/schemas/registry.json"
      - "scripts/check-chiodos-pheromone-relay-alert-assurance-archive-hardening.sh"
      - ".github/workflows/chiodos-pheromone-relay-alert-assurance-archive-hardening.yml"

permissions:
  contents: read

jobs:
  chiodos-pheromone-relay-alert-assurance-archive-hardening:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Check Chiodos pheromone relay alert assurance archive hardening
        run: bash scripts/check-chiodos-pheromone-relay-alert-assurance-archive-hardening.sh
