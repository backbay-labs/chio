name: Chio Runtime Orchestration

on:
  push:
    branches: [main]
    paths:
      - "crates/chio-chiodos-runtime/**"
      - "crates/chio-cli/**"
      - "crates/chio-kernel/**"
      - "crates/chio-runtime/**"
      - "crates/chio-spec-validate/**"
      - "examples/chio-3vendor/**"
      - "spec/schemas/chio-runtime/**"
      - "spec/schemas/chiodos/**"
      - "spec/schemas/registry.json"
      - "spec/schemas/MANIFEST.sha256"
      - "scripts/check-chio-runtime-orchestration.sh"
      - ".github/workflows/chio-runtime-orchestration.yml"
  pull_request:
    paths:
      - "crates/chio-chiodos-runtime/**"
      - "crates/chio-cli/**"
      - "crates/chio-kernel/**"
      - "crates/chio-runtime/**"
      - "crates/chio-spec-validate/**"
      - "examples/chio-3vendor/**"
      - "spec/schemas/chio-runtime/**"
      - "spec/schemas/chiodos/**"
      - "spec/schemas/registry.json"
      - "spec/schemas/MANIFEST.sha256"
      - "scripts/check-chio-runtime-orchestration.sh"
      - ".github/workflows/chio-runtime-orchestration.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: chio-runtime-orchestration-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  runtime-orchestration:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run Chio runtime orchestration gate
        run: bash scripts/check-chio-runtime-orchestration.sh
