name: chiodos-proof-package

on:
  pull_request:
    paths:
      - "crates/chio-chiodos/**"
      - "crates/chio-cli/**"
      - "crates/chio-federation/**"
      - "crates/chio-governance/**"
      - "crates/chio-workflow/**"
      - "crates/chio-selective-disclosure/**"
      - "crates/chio-conformance/**"
      - "examples/chiodos-3vendor/**"
      - "scripts/check-chiodos-proof-package.sh"
      - "spec/schemas/chiodos/**"
      - "spec/schemas/registry.json"
      - ".github/workflows/chiodos-proof-package.yml"
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: chiodos-proof-package-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  proof-package-gate:
    name: proof-package-gate
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry and target
        uses: Swatinem/rust-cache@v2

      - name: Run Chiodos proof-package gate
        run: bash scripts/check-chiodos-proof-package.sh
