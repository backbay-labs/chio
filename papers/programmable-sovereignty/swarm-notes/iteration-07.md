# Iteration 7

Focus: penultimate iteration. The MISSED list from iter-4 is fully addressed. Iter 6 found two existential threats (broken replay-corpus claim, 7/15 hallucinated bib entries). Iteration 7 pivots to (a) publication-readiness triage given the existential threats; (b) meta-review of the swarm itself -- which kinds of issues has the swarm systematically biased toward finding and which has it missed; (c) a fresh hostile read that treats every prior swarm finding as known and asks what's still uncaught after six iterations.

Agent sections appended below.

## Persona: Publication-readiness triage (post iter-6 existential threats)

**Q1 (publishable today as-is): No.** The paper has two integrity failures a hostile reviewer reaches in under five minutes -- (a) seven citations name authors who did not write the work, with `omegaTrustlets` pointing at a different paper than the one \S8 describes; (b) \S6's headline replay-corpus claim describes adversarial buyer-closure coverage the 50 fixtures do not provide, and the cited 277s figure is 276s of `cargo build`. Either alone reads as research misconduct on a charitable reading and as fabrication on a hostile one; disclosing in \S9 does not cure them because the affirmative claims still stand in the abstract, \S6, and \S8.

**Q2 (minimum fix set for arXiv without embarrassment).**
- **MUST fix pre-arXiv:** the seven WRONG bib entries (`omegaTrustlets`, `sampcertPLDI2025`, `cedarOOPSLA2024`, `cedarFSE2024`, `schneiderFMBC2025`, `sagaNDSS2025`, `eip7702`) re-derived against arXiv/DOI in one pass; six WOBBLE entries can land in v2 with a v1 endnote acknowledging citation hygiene is mid-pass.
- **\S6 replay-corpus prose MUST be rewritten,** not deferred to a \S9 limitation. The current text affirmatively misrepresents what the corpus tests; a \S9 disclaimer cannot undo an affirmative misstatement in \S6. The honest rewrite: separate the kernel-capability replay-gate (50 fixtures, smoke-coverage) from the Chiodos buyer-closure (currently unreplayed), report the 1.07s test wall-time not the 277s build-plus-test, and drop "byte-equivalent" framing for "manifest-stable across machines."
- **`omegaTrustlets` MUST be deleted,** not held by placeholder. \S8's "TEE boundary before any tool call" sentence cites a paper that does not exist at the URL; the sentence has to be rewritten or removed. A placeholder citation pointing at the wrong work is worse than no citation.
- **Figure 1 malformed-context bug MUST be fixed.** It is the only figure claiming fail-closed semantics and currently routes malformed contexts to allow. A reviewer reading the figure and \S5 together sees the contradiction.
- **Pass-5 surgical voice fixes (Hart trim, SAGA dual-choice, abstract clause, \S1 bullet 2, \S4 phrasing): YES, still land.** They are independent of iter-6 findings, low-risk, and the polish-diff records them as already applied -- the bib and \S6 rewrites do not unwind them.

**Q3 (publication sequence).** arXiv first is now off the table until the MUST-fix list lands. Recommended order: (1) MUST-fix pass (one focused week), (2) Walch pre-disclosure embargo (iter-3 strategic) on the corrected manuscript, (3) arXiv, (4) two-kernel federation work proceeds in parallel as the v2 substantive lift. Holding for the federation is wrong; the federation gates NDSS submission, not arXiv preprint.

**Q4 (honest convergence assessment).** Six iterations of swarm review have made the paper closer to a *trustworthy* v2 substantive revision, not closer to "ready to submit." The rhetorical surface is polished; the artifact-to-prose binding is broken in places polish passes cannot touch. The single decision the author must make next: accept that v1-on-arXiv is a one-week MUST-fix sprint, not a button-press from the current tree.

**Q5 (process change to catch the hallucination class).** Every citation added in a swarm pass must be re-derived from primary source (arXiv abstract page, DOI metadata, or venue proceedings) by a non-LLM step before the pass is marked complete, and every "first executable instance of X" claim must cite a primary text from X with page number. Iter-2 foundational-theory and iter-6 bib fact-checker found the same pattern (LLM confabulates plausible-sounding authors and overreaches theory framings) because no pass had a primary-source re-derivation gate.

**Highest-priority follow-up:** run a one-week MUST-fix sprint (seven bib re-derivations + \S6 rewrite + omegaTrustlets deletion + Figure 1 patch); arXiv ships from that branch, not from the current tree.

## Persona: Meta-swarm-quality reviewer (assessing the swarm itself)

The worst single bias is **citation-by-deference**: the swarm propagates each iteration's claims forward as facts without re-derivation, so a single agent's misread mints a "stabilized" finding that the next five iterations build atop.

Q1 (good at). Voice / rhetorical drift (every iteration), single-paragraph internal contradiction (iter-1 hostile triple-naming the Hart-vs-\S9 tension, iter-5 coherence auditor on three "load-bearing" referents), and prose-vs-artifact citation hygiene once attention turned there (iter-4 line-number archaeology, iter-6 fact-check of 7/15 confabulated authors). The swarm is excellent at re-reading the .tex.

Q2 (bad at). Mechanical re-execution. Across six iterations no agent has: (a) run `cargo test` and confirmed a specific Rust test passes (iter-4 only `grep`-cited existence of `treaty_runtime_hook_*`); (b) opened `Cargo.toml` to verify the crate dependency graph the paper implies; (c) actually run `lake build` against `formal/lean4/Chio/` (the polish-diff asserts success at three points; no agent has reproduced); (d) fact-checked the evaluation machine string -- "MacBook Pro 18,4 / M1 Max / 10 cores / 32 GB / Darwin 25.4.0" is internally consistent (M1 Max = 8P+2E = 10 cores; Darwin 25.4.0 maps to macOS 15.4) but no agent confirmed; (e) curled the DOIs in `bib.bib` (iter-6 checked author bylines, not URL resolution); (f) checked whether polish-diff's 44 BibTeX warnings are still present in the current build; (g) re-counted the 113 Lean declarations and 79 inventory entries after the pass-4 `enactAmendment` rename (the paper's headline numbers may have shifted).

Q3 (confirmation-bias patterns). Three: (i) the iter-1 hostile reviewer's "Hart-on-rfl" framing was re-affirmed by iter-1 PL skeptic, iter-2 foundational-theory, iter-4 synthesizer, iter-5 coherence auditor -- five concurrences with zero independent re-derivation of whether `treaty_admission_iff_predicate_intersection` is actually trivial; (ii) iter-4's "ladder mode 5 is `quorum_required` per Rust source" was cited by iter-5 and iter-6 as settled, no one re-opened `treaty.rs:681`; (iii) every iteration ends with a "highest-priority follow-up" that stacks on prior recommendations -- 18+ stacked recommendations and zero retractions; no agent has ever flagged another agent as wrong.

Q4 (the blind eye). **The Lean kernel actually checking what the .tex says it checks.** No agent has typed `lake build` and read the output. The chain "iter-4 said Rust matches -> polish-diff says lake build succeeds -> therefore the artifact is fine" is six hops of citation deference with zero verification.

**Highest-priority follow-up for iter-8:** commission an **artifact-execution agent**. Concrete charter: run `cargo build --workspace && cargo test --workspace`, run `lake build` from `formal/lean4/Chio/`, recount declarations and inventory entries against the paper's quantitative claims, curl every DOI in `bib.bib`, check whether `bibtex paper` still emits 44 warnings, and report exit codes and diffs. No prose critique. The swarm cannot finish honestly without one agent that executes rather than re-reads.

## Persona: Fresh hostile (post-swarm residue)

The single most damaging residue is that **the title still leads with "Programmable Sovereignty"** while six iterations have collectively dismantled every defensible reading of that phrase: the Hart claim retreats to condition (a) only (iter-2), the polity vocabulary is the buried lede that suppresses the regulator-facing headline (iter-1), and the body itself only uses the words "sovereignty" or "sovereign" eight times across ten sections (with three of those eight in \S7 attached to the very rhetoric the swarm has scoped down, and zero occurrences in \S1, \S6, \S8, \S9). The paper now reads as a paper whose title makes a claim its body explicitly refuses; a hostile PC chair sorts the submission by title alone before assigning reviewers, and "Programmable Sovereignty" is the worst possible discoverability vector for a paper whose actual contribution is a receipt-admission predicate substrate. No prior agent caught this because every iteration treated the title as a fixed point and audited only the body prose against it; iter-3 strategic explicitly recommended keeping the title as an "NDSS wedge," but no agent counted the residual sovereignty-density to test whether the wedge still cuts after pass-2/3/4 rhetorical tightening. Fix: rename to promote the existing subtitle ("Lean-Attestable Constitutions Over Capability-Bounded Federated Receipts") to head position and demote "Programmable Sovereignty" to a single closing-paragraph reframing in \S7.

Four additional residues no prior agent named:

1. **Table 4's assumption ledger has not been audited line-by-line for load-bearing vs. window-dressing.** All six rows carry equal hedging weight, but rows 1 (Ed25519/SHA-256) and 6 (legal entities outside the kernel) are *axiomatic* for the construction while row 4 (Lean inventory maintained with code changes) is *operational hygiene*; flattening them into one table signals the authors have not distinguished what the paper bets on from what it merely promises. No prior agent caught this because iter-4 named "Table 2 proof-to-code map unaudited" but never listed Table 4, and iter-5/6 hit different artifacts. Fix: split Table 4 into "Foundational assumptions" (rows 1, 2, 3, 6) and "Operational discipline" (rows 4, 5), with the latter marked as a process commitment rather than a residual cryptographic risk.

2. **The \S10 conclusion's "next concrete result is bilateral admission against an external regulator's co-signing key" has zero supporting evidence that any public regulator (US, EU, UK) has expressed interest in such an arrangement.** The sentence reads as a roadmap commitment to a counterparty that does not exist; iter-1's hostile reviewer caught the *contradiction* with the \S9 accreditation-gap bullet but not the *fabrication-by-implication* that some regulator is presumed willing. Every prior agent treated this as project-internal aspiration rather than reading it as an implicit external commitment that NDSS reviewers will fact-check against EU AI Office, NIST AI RMF, OFAC, and FCA public statements (none of which name Lean-attested receipts). Fix: rewrite to "the next concrete result is bilateral admission against a *willing* co-signing counterparty (industry consortium, sectoral SRO, or research-led pilot); regulator co-signing remains a multi-year accreditation track per the \S9 gap."

3. **The acmart `sigconf,anonymous,review,nonacm` formatting telegraphs an ACM venue while the body's citation density (Hart, Raz, Schauer, Crawford, Montevideo, Próspera, FTX, Tornado Cash) is FAccT/AIES-coded, not NDSS/USENIX-coded.** A PC chair seeing the ACM SIG conference template alongside legal-philosophical and political-economy citations either (a) routes the paper to a FAccT/AIES reviewer pool that will reject on technical formal-verification grounds, or (b) routes it to an NDSS-style pool that will reject on framing grounds. Iter-3 strategic recommended FAccT + NDSS as co-conferences but never examined whether the *formatting choice itself* commits to one community at the expense of the other. Fix: produce two physical formats from one source -- USENIX style for a security submission, acmart for a FAccT submission -- and decide the v1 arXiv preprint format based on which review timeline lands first.

4. **The "two-paper strategy" (long paper + bilateral-receipt short paper) has not been checked against NDSS or USENIX Security simultaneous-submission policies.** Both venues forbid concurrent submission of substantially overlapping content; iter-3 comparator named "salami slice" as a self-plagiarism *reviewer-perception* risk but never cited the actual NDSS 2026 or USENIX Security 2026 CFP language that triggers desk-reject independent of reviewer judgment. No prior agent caught this because the swarm's strategic discussion was about content separability, not policy compliance. Fix: read the current NDSS 2026 and USENIX Security 2026 CFPs side-by-side, identify which venue permits a workshop-published precursor and which forbids it, and lock the short-paper venue to the one whose policy explicitly admits the long-paper relationship -- before drafting any further \S4 freestanding theorem.

Highest-priority follow-up: rename the paper title to promote the existing subtitle to head position and demote "Programmable Sovereignty" to a single \S7 closing reframing, because the title is the only surface the entire swarm took as fixed and is the residue most likely to determine reviewer assignment and first-impression posture before any body content is read.

## Iteration summary

Convergent: the paper is NOT publishable as-is; the triage agent prescribes a one-week MUST-fix sprint (seven WRONG bib entries re-derived, \S6 replay-corpus prose rewritten, omegaTrustlets deleted, Figure 1 fixed) before arXiv; the meta-swarm agent diagnoses the systematic cause as citation-by-deference and absence of an artifact-execution gate (no swarm agent has run `cargo test`, `lake build`, or DOI resolution across six iterations); the fresh-hostile-residue agent identifies five new issues no prior iteration caught and pushes hardest on the title-vs-body density mismatch (8 mentions of "sovereignty" in body vs the title's prominent claim). Divergent on title: triage treats renaming as a future v2 move; fresh-hostile advocates renaming pre-arXiv to align title with the polished body; round-2 tactical advocated keeping the title as rhetorical wedge -- the wedge argument was strongest five passes ago, the body has tightened so much the title now overpromises. Across all three agents the iteration-8 charter writes itself: commission a single artifact-execution agent (cargo build/test, lake build, curl DOIs, recount declarations and inventory, check BibTeX warnings, verify machine-spec consistency) before the swarm closes, because no prior iteration has actually RUN anything.
