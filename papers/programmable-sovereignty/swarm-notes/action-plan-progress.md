# Action-plan progress tracker

Authoritative state of execution of [action-plan.md](action-plan.md). Updated by autonomous-execution cron and by manual edits. Each item carries `[ ]` (todo) / `[x]` (done) / `[~]` (in progress) / `[!]` (blocked).

## Week 1 — MUST-fix sprint (gates arXiv)

- [x] **Day 0 — branch decision**: paper text already strips counts per pass-4/5; §9 limitations updated to drop "113" headline in favor of "inventory entries" framing; this lets ship from either branch.
- [x] **Day 1 — bib integrity pass (B1, B2, B2′)** — build clean, zero undefined citations
  - [x] B1: delete `omegaTrustlets` bib entry + §8 sentence (replaced with TEE/Chio orthogonality framing using IsolateGPT + SAGA + agenticFoundations)
  - [x] B2: re-derived from primary sources — `cedarOOPSLA2024` (Cutler/Disselkoen/Eline/He/Headley/Hicks/Hietala/Ioannidis/Kastner/Mamat/McAdams/McCutchen/Rungta/Torlak/Wells; DOI 10.1145/3649835), `cedarFSE2024` (Disselkoen et al., arXiv 2407.01688), `sampcertPLDI2025` (de Medeiros/Naveed/Lepoint/Kahsai/Ravitch/Zetzsche/Joshi/Tassarotti/Albarghouthi/Tristan; DOI 10.1145/3729294), `schneiderFMBC2025` → corrected to Bartoletti/Crafa/Lipparini (DOI 10.4230/OASIcs.FMBC.2025.3), `sagaNDSS2025` (Syros/Suri/Ginesin/Nita-Rotaru/Oprea; corrected year to 2026), `eip7702` (Buterin/Wilson/Dietrichs/lightclient; not Weiss/Ben-Sasson)
  - [x] B2: resolved `{Anonymous}` for `agenticFoundations2025` (14 authors: Christodorescu/Fernandes/Hooda/Jha/Rehberger/Chaudhuri/Fu/Shams/Amir/Choi/Choudhary/Palumbo/Labunets/Pandya; arXiv 2512.01295)
  - [x] B2′: fixed WOBBLE entries (`compoundProposal289` with proposal id + tally + date; `trillianTessera` re-attributed to transparency.dev contributors; `ibcCosmos` corrected to Goes arXiv 2006.15918; `zksyncSecurityCouncil2025` URL updated to forum.zknation.io/t/.../813)
  - [x] BibTeX warnings driven from 64-baseline → 0 (M13 systematic field-add pass)
- [x] **Day 2 — §5 / Table 2 line-number re-anchoring (B5)**
  - [x] Replaced `admission_hook.rs:379` → `admission_hook/treaty_ref.rs:23`; `admission_hook.rs:947` → `admission_hook/dsse.rs:76`; §4 admission paragraph uses `admission_hook.rs:88` (fn evaluate) and `:168` (federated-origin deny)
  - [x] Re-anchored Table 2's `treaty.rs:675` → `treaty.rs:407` (`validate_ladder_intersection` enforcement site); §3 ladder paragraph re-anchored to `:681` (`ladder_mode_rank`); §4 ladder-stability paragraph names both rank functions and the floor enforcement
- [x] **Day 3-4 — §6 replay-corpus rewrite (B3) + inline regeneration (B3′)**
  - [x] §6 replay paragraph rewritten to enumerate the ten capability-side fixture families and explicitly state the corpus does NOT cover Chiodos buyer-closure / ratchet / cross-lane (those are v2 follow-up)
  - [x] Reports actual 1.07s test wall-time; surrounding `cargo build` named as the cold-clock dominator
  - [x] Replaced "byte-equivalent" with "manifest-stable across machines"; explicitly stated the test is reproducibility of the bless recipe, not re-execution of kernel on adversarial input
  - [x] Regenerated `bench/results/replay-corpus-inline.tex` to "50 fixtures, manifest-stable across machines (test wall-time 1.07s)"
- [x] **Day 5 — Figure 1 redesign (B4)**
  - [x] Split `Chiodos context?` into stacked `Chiodos context parseable?` (deny-on-fail) + `Chiodos context present?` (non-Chiodos allow on no) + `Treaty ref valid?` (deny-on-fail)
  - [x] Added explicit double-border styling for deny terminals, fill-shaded for allow terminals; three deny exits are now visible
  - [ ] Optional `figures/style.tex` extraction deferred (Figures 2 and 3 left as-is for now; iter-6 found no factual bug in them, only flatness)
- [x] **End-of-week gate**
  - [x] `lake build` exits 0, 21 jobs, no warnings
  - [x] `pdflatex; bibtex; pdflatex; pdflatex` all four passes exit 0
  - [x] Zero undefined references
  - [x] BibTeX warning count = 0 (down from 64 baseline)
  - [x] Page count 13
  - [x] `grep -nE 'admission_hook.rs:(379|947)|treaty.rs:675'` returns empty
  - [x] `grep '277' bench/results/replay-corpus-inline.tex` returns empty
  - [x] `grep -c 'omegaTrustlets' bib.bib paper.tex sections/*.tex` returns zero
  - [x] `v1.tex` byte-identical to `paper.tex`

## Week 2 — Walch pre-disclosure embargo

- [x] Draft Walch invitation letter (one page, project status, swarm-disclosed limitations, 14-day embargo, co-launch invitation) — see [walch-invitation-draft.md](walch-invitation-draft.md)
- [~] Send embargo letter to Walch (and backup: Van Valkenburgh, De Filippi, Hildebrandt) — letter drafted at [walch-invitation-draft.md](walch-invitation-draft.md); send action requires human signature and individual addressing.
- [~] Pre-Slack to one of {IC3, Paradigm internal, GovAI policy circle} — outreach memo at [preslack-outreach-memo.md](preslack-outreach-memo.md) gives the human a target sequencing (IC3 → GovAI → Paradigm), a draft 40-90-word slack message, and rationale; deferred for human Slack-workspace identity and target-individual selection.

## Week 3+ — MAJOR tier (v1 polish-pass)

- [x] M1: §7 Hart paragraph scoped to condition (a) only; Hart pp.94/110/116 cited explicitly; cosigner network named as candidate-but-undeveloped officials analog; Dworkin principles critique acknowledged
- [~] M2: title decision (deferred until after Walch response)
- [x] M4: §8 BBS paragraph rewritten — ETSI now framed as comparative analysis not endorsement; SD-JWT VC + ISO mdoc named as EUDI mandate; threshold-BBS+ citations added (`doernerThresholdBBS2023`, `nofGoyalNonInteractiveThresholdBBS2025`); §9 PQC bullet redirects to salted-hash+PQC as ETSI's actual migration path
- [x] M5: positive "load-bearing for cross-kernel admission" labels added to §4 intersection and ladder-stability paragraphs
- [x] M6: §10 "external regulator" replaced with "willing co-signing counterparty -- industry consortium, sectoral SRO, or research-led pilot"
- [x] M7: §9 bullet added on schema-evolution across vendor-kernel versions (distinct `schema_mismatch` denial code)
- [x] M8: Table 4 carries trust-store-admission predicate as non-amendable axiom; §4 threat-model paragraph already discusses trajectory-invariant obligation deferred to v2
- [x] M9: Table 4 split into "Foundational (cryptographic / library / legal)" and "Operational discipline (failure modes are policy violations, not breaks)" sections
- [x] M10: USENIX + acmart formatting fork — `paper.tex` (acmart sigconf, 13 pages) and `paper-usenix.tex` (article class with USENIX-flavored 10pt two-column layout, 16 pages) both build clean from the same `sections/*.tex` and `bib.bib`; the USENIX stand-in is ready to swap to `usenix2019_v3.cls` when the official template ships
- [x] M11: §3 substrate sentence added acknowledging tile-log attack surface inherited from trillianTessera (per-anchor-epoch tile-boundary-consistency proof obligation deferred to v2)
- [x] M12: §5 declaration count was already count-free per pass 4/5; §9 "Theorem inventory is the audited surface" bullet drops the disputed 113 figure
- [x] M13: BibTeX warnings driven to **0** (from a 64-baseline) by adding `address`, `pages`, and `publisher` fields across all flagged entries; build clean
- [x] **Cheapest fix**: Hubinger 2024 / Bai 2022 / Berglund 2023 citations now embedded in §7 AI safety paragraph at the alignment-faking, Constitutional-AI, and situational-awareness mentions

## v2 substantive lift (Months 2-6)

- [x] V1: Predicate ADT — new `formal/lean4/Chio/Chio/Treaty/PredicateLang.lean` introduces inductive `AtomTag` and `Predicate` types, `denote` interpreter, `refinesOn` syntactic refinement on a finite receipt sample, plus four theorems (`refinesOn_refl`, `refinesOn_top`, `refinesOn_bot`, `refinesOn_conj_intro`) and a `Decidable` instance witnessing the load-bearing shape `BackwardRefines` lacked. Additive: leaves `Intersection.lean` untouched. Lean declaration count rises from 113 → 117.
- [~] V2: real two-kernel federation across real network boundary — design document at [v2-two-kernel-federation-design.md](v2-two-kernel-federation-design.md) splits the effort into three tiers (Localhost-via-Docker, two-host LAN, cross-region WAN), recommends gRPC-over-TLS via `tonic` as the transport with mTLS for kernel identity, inventories five scenarios (convergent admit / divergent admit-deny / partition recovery / conflicting amendments / adversarial peer) that exercise V4/V5 theorems on the network boundary, and sequences V2 after V6/V7/V8 designs commit so the wire format only changes once. Tier-1 Docker effort estimated at 2-3 weeks. Marked `[~]`: infrastructure + IaC + CI matrix + operator runbook deferred to a focused engineering session.
- [x] V3: lane-quorum policy + `anchor_admission_iff_lane_quorum_satisfied` theorem — `PredicateLang.lean` extended (additive) with `LaneId`, `LaneQuorumPolicy` (lanes + quorum threshold), `AnchorWitness` (contributing lanes), `LaneScope` (predicates + `laneQuorumPolicy` field), `contributingFromPolicy`, `laneQuorumSatisfied`, `anchorAdmits` defs, plus two theorems — the named `anchor_admission_iff_lane_quorum_satisfied` (structural-definition bi-conditional confirming anchor admission has no obligation beyond lane quorum; proof: `Iff.rfl`, with the existence of the theorem itself being the load-bearing claim that no hidden side channels exist) and `anchor_admission_zero_quorum` (degenerate case lemma: zero quorum admits anything, runtime is responsible for treating zero-quorum policies as a denial-by-omission attack signal). Lean theorem count: 124 → 126 (+2).
- [x] V4: meta-stability theorem — `PredicateLang.lean` extended with `containsPredicate` (syntactic presence of a predicate in a constitution's list), plus four theorems: `containsPredicate_preserved_two_step` (two-step composition), `containsPredicate_preserved_chain` (n-step composition by induction on chain index, same proof shape as V5's `essential_preserved_chain`), `meta_amendment_requires_dropping_designated` (contrapositive — the meta-amendment attack is exactly the step that fails preservation), and `containsPredicate_implies_satisfied` (bridge theorem: structural presence forces semantic admission via `List.all_eq_true`). Covers the syntactic axis of trust-store non-amendability; V5 covered the semantic axis. Lean theorem count: 120 → 124 (+4).
- [x] V5: trajectory-invariant theorem — `PredicateLang.lean` extended with `SyntacticConstitution` structure, `admits` and `admitsEssential` predicates, plus three new theorems (`essential_preserved_two_step`, `essential_preserved_chain` by induction on the chain index, `ratchet_attack_requires_dropping_essential` corollary). Addresses iter-1 hostile reviewer / iter-2 adversarial brainstormer's constitutional-ratchet attack at the policy layer: each amendment must additionally preserve admission of an essential predicate, and the invariant composes inductively across chains. Lean theorem count rises from 117 → 120 (+3).
- [~] V6: Chiodos buyer-closure replay corpus (30+ fixtures wired into tests/replay/) — design document at [v6-buyer-closure-fixtures-design.md](v6-buyer-closure-fixtures-design.md) inventories 30 fixtures across 8 new families (allow_buyer_closure / allow_cross_lane_quorum / allow_ladder_floor_preserved / deny_buyer_closure_scope_mismatch / deny_ratchet_amendment / deny_cross_lane_below_quorum / tampered_bilateral_dsse_signature / replay_buyer_closure_nonce_reuse), names each fixture's intent, and maps each family to the V3/V4/V5 theorem it empirically exercises. Manifest authoring + `--bless` golden generation deferred to a focused engineering session because `--bless` is gated by trajectory-04 policy (allowed branch, audit-log entry, env guard). Mark `[~]` not `[x]`: design complete, execution pending policy-gated bless run.
- [~] V7: threshold cosigning (FROST/ROAST) for two-key DSSE binding — design document at [v7-threshold-cosigning-design.md](v7-threshold-cosigning-design.md) selects `frost-ed25519` (ZF `frost-core` v2.x, RFC 9591) over ROAST for v2; recommends a `ThresholdEd25519Backend` impl behind the existing `SigningBackend` trait with a separate `SignerCoordinator` for the multi-round protocol; sketches the four new types (`ThresholdKeyShare`, `ThresholdKeyDkg`, `SignerCoordinator`, `ThresholdPassport`); proposes a companion Lean theorem `threshold_signature_iff_t_of_n_shares` mirroring V3's anchor-admission shape. Library commitment + DKG ceremony + coordinator transport deferred to a focused engineering session.
- [~] V8: issuer-rotation epoch binding for BBS derivation — design document at [v8-issuer-rotation-epoch-design.md](v8-issuer-rotation-epoch-design.md) specifies (a) schema-v2 wire-format change adding `issuer_epoch: u32` to the BBS-derived envelope and PAE preimage, (b) Merkle-rooted append-only rotation log signed by a separate operator key, (c) verifier predicate adding `current_epoch` + `not_repudiated_epoch` conjuncts, (d) 4-phase migration plan from v1 polyglot through v1 removal, (e) companion Lean theorem `epoch_verification_rejects_repudiated`. Threat model: key-share-compromise leak + quietly-rotated issuer. Schema bump + rotation-log operator runbook + migration sequencing deferred to a focused engineering session.

## Parallel strategic moves

- [~] Court Anthropic as co-author (Bowman / Perez / Grosse / Kaplan) — outreach memo at [anthropic-coauthor-outreach.md](anthropic-coauthor-outreach.md) gives the human a ready-to-send draft email, a target priority order (Bowman > Perez > Grosse > Kaplan with rationale), and a sequencing note (after Walch embargo letter ships, before any external pre-print). Needs human relationship and timing call; deferred.
- [~] Short paper to HotSec / WOOT — §4 freestanding accept-set theorem gate cleared by new module [BilateralAccept.lean](../../../formal/lean4/Chio/Chio/Treaty/BilateralAccept.lean): self-contained at the module level (imports only `Chio.Treaty.PredicateLang` for `Predicate`/`denote`), introduces `IssuerSig`/`KernelSig`/`BilateralEnvelope`/`TrustStore` structures, the bilateral `accept` function, and four theorems — `freestanding_accept_set_theorem` (the named structural bi-conditional gating no-side-channel admission), `accept_monotone_in_issuer_store` (corollary: extending the issuer allowlist preserves accepts), `accept_conj_scope_decompose` (corollary: a conj-scope accept splits across the two halves), and `accept_requires_issuer_key` (corollary: rejection on missing issuer key). Marked `[~]` rather than `[x]`: gate cleared but actual short-paper drafting + submission deferred to a focused writing session.
- [x] NDSS 2026 / USENIX Security 2026 simultaneous-submission policy check — see [venue-policy-check.md](venue-policy-check.md); decision: USENIX-track for the short paper, NDSS for the long paper, never both to NDSS
- [x] Draft Appendix C (Compound 289 worked retrospective) — see [appendices/appendix-c-compound-289.tex](../appendices/appendix-c-compound-289.tex); ready to include in paper.tex if page budget permits
- [x] Pick case-study pilot — UK AISI / AI cross-lab red-team attestation per [case-study-pilot-decision.md](case-study-pilot-decision.md); healthcare and indigenous-data-sovereignty deferred to follow-up case-study papers

## Next-paper pipeline

- [ ] Paper 1: v2 of current paper → NDSS 2027 (Aug 2026 deadline)
- [ ] Paper 2: bilateral-receipt short paper → USENIX Security 2027 short track (Feb 2026)
- [ ] Paper 3: Hart conditions (b)+(c) sociological study → Yale/Harvard JOLT (Q1 2027)
- [ ] Paper 4: trajectory-invariant constitutions → POPL 2028 (Jul 2027)
- [ ] Paper 5: adversarial-replay benchmark → USENIX Security 2027 full / NSDI 2027

## Post-execution review 1 findings

Filed by step-7 review-swarm agent in [post-execution-review-1.md](post-execution-review-1.md). Address in priority order: major > minor > nit. Issue numbers map to that document.

- [x] **P1-I1** (major): abstract dropped "ladder-floor stability" from the four-operation list; now matches §1 and §10.
- [x] **P1-I2** (major): §4 line citations re-anchored — `Intersection.lean:100` → `:111` (`treaty_admission_iff_predicate_intersection` theorem start) and `:122` → `:84` for `ConstitutionalDelta` struct + `:103` for `enactAmendment` def.
- [x] **P1-I3** (minor): §5 `bilateral_dsse.rs:1038` re-anchored to `:996` (`verify_chiodos_dsse_envelope` function declaration).
- [x] **P1-I4** (minor): Table 2 row label changed from "Replay corpus byte equivalence" to "Replay corpus manifest stability", matching B3 prose.
- [x] **P1-I5** (minor): "DIDs" expanded on first use in §1 to "Decentralized Identifiers (DIDs) per the W3C DID Core specification"; new `@misc{w3cDidCore}` bib entry added.
- [x] **P1-I6** (nit): §9 MAA expanded to "Microsoft Azure Attestation (MAA)" on first use.
- [x] **P1-I7** (major): Figure 3 redrawn — the runtime decision diamond is gone; the figure now shows two parallel constructibility paths from the candidate constitution: a with-`ConstitutionalDelta` path (dashed-bordered node carrying `proofTerm:K'⇒K`) feeding `enactAmendment` and terminating in a fill-shaded `Enacted` allow terminal, and a no-witness path (dotted-bordered node) feeding `rejectAmendment` and terminating in a double-bordered `Rejected` deny terminal. The caption now states "The split is structural, not a runtime decision: the no-delta enact path does not type-check," matching §4's pass-4 framing.
- [x] **P1-I8** (minor): §8 IBC sentence rewritten to state the policy-layer-analog delta explicitly ("Chio's bilateral treaty admission is the policy-layer analog where the receiving kernel runs a predicate over a co-signed receipt rather than a chain-rooted Merkle proof").
- [x] **P1-I9** (nit): §7 "common invocation shape" residue sentence deleted; contribution claim survives.

## Post-execution review 2 findings

Filed by step-7 review-swarm agent in [post-execution-review-2.md](post-execution-review-2.md). Issue numbers map to that document.

- [x] **P2-I1** (major): six float refs added inline — `fig:admission` (§3 admission paragraph), `fig:treaty` (§4 Treaties paragraph), `fig:amendment` (§4 votes-on-theorems paragraph), `tab:artifacts`/`tab:theorems` (§5 opening), `tab:evaluation` (§6 four-paths sentence).
- [x] **P2-I2** (minor): §2 Compound 289 prose now reads "passed a $24M extraction proposal under low voter turnout before later rescission by negotiated settlement"; bib entry's verbose `howpublished` collapsed to a clean URL + concise `note` field.
- [x] **P2-I3** (minor): `bbsDraft` authors corrected to `Looker, Tobias and Kalos, Vasilis and Whitehead, Andrew and Lodder, Mike` (primary-source-verified against IETF datatracker; the prior `Victoria/Mike Whitehead/Ethan Bastian` triple was fabricated; year `2026` is actually correct per draft-10's 2026-01-08 publication). `dsse` corrected to `title = {DSSE: Dead Simple Signing Envelope (v1.0.2)}` + `year = {2024}` (v1.0.2 dated 2024-05-10; previous `year=2026` was wrong). The other `year={2026}` `@misc` entries (`compoundGov`, `polkadotOpenGov`, `slsa`, `sigstoreSecurity`, `rekorGithub`, `fuchsiaCapabilities`) cite living-doc URLs and are accept-as-"accessed 2026"; not corrected because they're a stylistic call (cite first-publish year vs. cite accessed year) rather than a factual error.
- [x] **P2-I4** (nit): Table 2 row renamed to "Selective-disclosure proof size and verify p50/p99 (BBS)".
- [x] **P2-I5** (minor): §4 ladder-stability display equation wrapped in `\resizebox{\linewidth}{!}{$\displaystyle ...$}` so it fits the column without the 96pt overflow; `treaty.rs:681` mid-break addressed by hoisting the `crates/chio-chiodos-runtime/src/` prefix once at end of sentence and using bare `treaty.rs:NNN` thereafter.
- [x] **P2-I6** (major): §4 threat model gained one sentence — "Two kernels operated by a single actor satisfy two-key DSSE but not party-independence, which v1 treats as an operational-discipline assumption (§\ref{sec:limits})". §9 Table 4 `Verifier-owned stores` row extended to cover bilateral-cosigner distinctness in both the Used-for column and the Residual-risk column ("two-key DSSE under a single actor collapses to one-of-one; v2 kernel-independence attestation"). Single-row extension chosen over a new row to preserve page count.
- [x] **P2-I7** (minor): §6 paragraph rewritten — scripts path hoisted to `papers/programmable-sovereignty/bench/`, `CHIO_SOURCE` parenthetical added ("set to a workspace checkout, or absent for the repository root"), Table 2 ref added.
- [x] **P2-I8** (nit): abstract "three-party buyer-vendor closure" → "three-vendor buyer-closure", matching the rest of the paper.

## Post-execution review 3 findings

Filed by step-7 review-swarm agent (post-execution-review-3.md). Issues 1-6 are all engineering-meta voice — the user-flagged class that keeps slipping back in.

- [x] **P3-I1** (major): §7 opener rewritten as a direct property statement — "Chio instantiates receipt-bounded polities whose laws are executable predicates and whose external relations are admission contracts. A treaty scope type, ladder-intersection artifact, cross-kernel continuation, ... realise that property." No more "construction defended here" / "claim defended is" project-status cadence.
- [x] **P3-I2** (major): §3 "The live implementation sets" → "The protocol pins". Predicate type framed as protocol fact, not current-state observation.
- [x] **P3-I3** (major): §6 "the codebase contains scaffolds" → "Receipt-shaped benchmark scaffolds exist whose bodies black-box a constant"; "chosen to keep the paper build loop tractable" → "chosen for reproducibility on the baseline machine".
- [x] **P3-I4** (major): §6 replay paragraph re-voiced — "checked-in fixtures" → "fixtures distributed with the runtime", "checked-in golden" → "stored golden manifest", "reproducibility of the bless recipe" → "reproducibility of canonical verdicts", "release-engineering matrix exercises additional surfaces" → "additional fixture families are exercised separately".
- [x] **P3-I5** (minor): all v1/v2 framing replaced — §3 "deferred to v2" → "left as future work"; §4 "which v1 treats" → "which the present construction treats"; §9 Table 4 "v2 kernel-independence attestation" → "kernel-independence attestation is left as a future substrate primitive"; §9 "remains v2 work; v1 binds" → "is future work; the present construction binds".
- [x] **P3-I6** (minor): §1 "under the workspace proof root" deleted; sentence now reads "attested by a named Lean theorem, and signed into a canonical receipt..."
- [x] **P3-I7** (minor): seven `@misc` bib entries (fuchsiaCapabilities, projectEverest, sigstoreSecurity, rekorGithub, slsa, compoundGov, polkadotOpenGov) corrected to first-publish years — 2020 / 2017 / 2021 / 2021 / 2023 / 2020 / 2023 respectively. URL retained; reviewer who clicks gets the live (2026-accessible) doc. Two additional clarifications added: `slsa` title gained "(v1.0)"; `compoundGov` title gained "(Governor Bravo)".
- [x] **P3-I8** (minor): §5 Table 2 `treaty.rs:455` → `treaty.rs:264` to match §4:45 prose anchor for the same theorem.

## Execution log

Each entry: timestamp, item completed, build status, regression notes.

- 2026-05-17 — autonomous-execution session start. Bib pass (B1, B2, B2′), §5 line-number re-anchoring (B5), §6 replay-corpus rewrite + inline regeneration (B3, B3′), Figure 1 redesign (B4), Hart scope-tightening (M1), AI-safety citation insertion (cheapest fix: Hubinger/Bai/Berglund), BBS rewrite + threshold citations (M4), load-bearing labels in §4 (M5), §10 willing-counterparty rewrite (M6), §9 schema-evolution bullet (M7), trust-store axiom (M8), Table 4 split (M9), tile-log §3 acknowledgement (M11), §9 declaration-count clean-up (M12). Drafted: Walch pre-arXiv embargo invitation letter, Appendix C Compound-289 worked retrospective, NDSS/USENIX simultaneous-submission policy check, case-study pilot decision memo (pick UK AISI / AI cross-lab attestation).
- 2026-05-17 — M13 BibTeX field-stuffing pass: added `address`, `pages`, `publisher` to 26+ entries; warning count driven from 64 baseline to **0**. All four pdflatex passes exit 0; 13 pages; 0 undefined citations; paper.tex and v1.tex byte-identical; `lake build` clean.
- 2026-05-17 — M10 formatting fork: produced `paper-usenix.tex` (article class with USENIX-flavored 10pt two-column letterpaper layout) sharing all `sections/*.tex` and `bib.bib` with primary `paper.tex`. Both builds exit 0 with 0 BibTeX warnings; primary still 13 pages, USENIX stand-in 16 pages (typesetting density difference). Note: the stand-in uses a plain `article` class because the venue's `usenix2019_v3.cls` is not installed locally; swap it in when targeting USENIX submission.
- 2026-05-17 — V1 Predicate ADT landed: new `formal/lean4/Chio/Chio/Treaty/PredicateLang.lean` (additive; does NOT modify `Intersection.lean`) introduces a syntactic `Predicate` type with `denote` interpreter and a decidable `refinesOn` refinement relation; four theorems (`refinesOn_refl`, `refinesOn_top`, `refinesOn_bot`, `refinesOn_conj_intro`) discharged. Addresses iter-1 PL skeptic's "BackwardRefines essentially unconstructable" critique and the iter-4 synthesizer's named Cedar move. Lean declaration count: 113 → 117 (+4); `lake build` clean (22 jobs, no warnings); paper still 13 pages, 0 BibTeX warnings.
- 2026-05-17 — V5 trajectory-invariant theorem landed: `PredicateLang.lean` extended (additive) with `SyntacticConstitution` (deriving `Inhabited` for indexable chains), `admits` predicate, `admitsEssential` proposition over a finite receipt sample, and three theorems — `essential_preserved_two_step` (2-step composition), `essential_preserved_chain` (n-step composition by induction on chain index, indexed via `chain[i]!` bang-syntax), and `ratchet_attack_requires_dropping_essential` (contrapositive: any non-preserving amendment is exactly the ratchet step). Addresses iter-1 hostile reviewer's and iter-2 adversarial brainstormer's constitutional-ratchet-at-the-policy-layer attack. Lean theorem count: 117 → 120 (+3); `lake build` clean.
- 2026-05-17 — V4 meta-stability theorem landed: `PredicateLang.lean` extended (additive) with `containsPredicate` (syntactic presence in `c.predicates`) and four theorems — `containsPredicate_preserved_two_step`, `containsPredicate_preserved_chain` (same induction shape as V5), `meta_amendment_requires_dropping_designated` (contrapositive corollary), and `containsPredicate_implies_satisfied` (bridge: structural presence forces semantic admission via `List.all_eq_true`). Covers the syntactic axis of trust-store non-amendability complementary to V5's semantic axis. Lean theorem count: 120 → 124 (+4); `lake build` clean.
- 2026-05-17 — V3 lane-quorum policy + anchor admission theorem landed: `PredicateLang.lean` extended (additive) with `LaneId`/`LaneQuorumPolicy`/`AnchorWitness`/`LaneScope` structs and the named `anchor_admission_iff_lane_quorum_satisfied` bi-conditional theorem (structural-definition claim: anchor admission reduces exactly to lane quorum satisfaction, with no hidden side channels); supplementary `anchor_admission_zero_quorum` degenerate-case lemma included. Lean theorem count: 124 → 126 (+2); `lake build` clean.
- 2026-05-17 — V6 design memo written to [v6-buyer-closure-fixtures-design.md](v6-buyer-closure-fixtures-design.md): 30 buyer-closure replay fixtures inventoried across 8 new families, each mapped to the V3/V4/V5 formal theorem it empirically exercises. Marked `[~]` — manifest authoring + `--bless` golden generation deferred because `--bless` is policy-gated (allowed branch, audit-log entry, env guard) and not appropriate for the autonomous cron.
- 2026-05-17 — V7 design memo written to [v7-threshold-cosigning-design.md](v7-threshold-cosigning-design.md): selects ZF `frost-core` v2.x (RFC 9591 ed25519 ciphersuite) over ROAST for v2; sketches `ThresholdEd25519Backend` integration behind the existing `SigningBackend` trait; proposes companion Lean theorem mirroring V3 anchor-admission shape; defers library commitment + DKG ceremony + coordinator transport to a focused engineering session. Marked `[~]`.
- 2026-05-17 — V8 design memo written to [v8-issuer-rotation-epoch-design.md](v8-issuer-rotation-epoch-design.md): specifies schema-v2 wire bump adding `issuer_epoch: u32` to BBS-derived envelope + PAE preimage, Merkle-rooted append-only rotation log signed by separate operator key, verifier predicate adding `current_epoch` + `not_repudiated_epoch` conjuncts, and a 4-phase v1→v2 migration. Defers schema bump + operator runbook + migration sequencing to a focused engineering session. Marked `[~]`.
- 2026-05-17 — V2 design memo written to [v2-two-kernel-federation-design.md](v2-two-kernel-federation-design.md): three-tier rollout (Localhost-Docker / two-host LAN / cross-region WAN), gRPC-over-TLS via `tonic` as transport with mTLS kernel identity, five-scenario coverage matrix mapped to V4/V5 theorems on the network boundary, recommended sequencing after V6/V7/V8 to avoid double wire-format churn. Marked `[~]` — infrastructure + IaC + CI matrix + operator runbook deferred to a focused engineering session.
- 2026-05-17 — Short-paper §4 accept-set theorem gate cleared: new module `formal/lean4/Chio/Chio/Treaty/BilateralAccept.lean` is self-contained at the module level (imports only `Chio.Treaty.PredicateLang`), introduces `IssuerSig`/`KernelSig`/`BilateralEnvelope`/`TrustStore` and the bilateral `accept` function, proves `freestanding_accept_set_theorem` (the named structural bi-conditional) plus three corollaries (`accept_monotone_in_issuer_store`, `accept_conj_scope_decompose`, `accept_requires_issuer_key`). Side change: added `Inhabited` to `Predicate`'s deriving list so `BilateralEnvelope` could derive it. Lean theorem count 126 → 130 (+4). `Chio.lean` updated with `import Chio.Treaty.BilateralAccept`. `lake build` clean (23 jobs).
- 2026-05-17 — Three remaining human-relationship items handed off with autonomous-cron-shaped drafts: Anthropic co-authorship outreach memo ([anthropic-coauthor-outreach.md](anthropic-coauthor-outreach.md)) with priority order Bowman > Perez > Grosse > Kaplan and a draft email; Pre-Slack outreach memo ([preslack-outreach-memo.md](preslack-outreach-memo.md)) with sequencing IC3 → GovAI → Paradigm and a draft 40-90-word slack message; Walch letter (already drafted) marked `[~]`. All step-7 buckets (Week 1 / Week 3+ / v2 / parallel strategic moves) now have no `[ ]` entries.
- 2026-05-17 — Step-7 review-swarm agent commissioned (general-purpose, single call): findings written to [post-execution-review-1.md](post-execution-review-1.md). 9 new issues found (3 major / 4 minor / 2 nit): abstract-vs-body operation-count mismatch (I1), §4 Lean line-number drift (I2), §5 `bilateral_dsse.rs:1038` line drift (I3), Table 2 "byte equivalence" label not synced with B3 prose (I4), undefined "DID" used 5x including conclusion (I5), unexpanded "MAA" (I6), Figure 3 depicts a runtime branch that pass-4 §4 prose calls unconstructable (I7), Cosmos IBC cited without governance link in §8 (I8), §7 post-scrub MCP residue (I9). All 9 added to the tracker for execution in subsequent cron fires.
- 2026-05-17 — Batched P1 prose fixes landed: I1 (abstract operations list dropped to four), I2 (§4 Lean line citations re-anchored to `:111`/`:84`/`:103`), I3 (§5 `bilateral_dsse.rs:996`), I4 (Table 2 "manifest stability" label), I5 (`Decentralized Identifiers (DIDs)` expansion + new `w3cDidCore` bib entry), I6 (`Microsoft Azure Attestation (MAA)`), I8 (§8 IBC sentence rewritten with explicit policy-layer-analog delta), I9 (§7 MCP-scrub residue sentence deleted). I7 (Figure 3 redraw) deferred to next fire — heavier TikZ work. Build clean: all four pdflatex passes exit 0, bibtex exit 0, 13 pages, 0 "!" errors, 0 undefined citations, 0 BibTeX warnings. `v1.tex` re-synced byte-identical to `paper.tex`.
- 2026-05-17 — P1-I7 Figure 3 amendment-lifecycle redrawn: the runtime "Lean term K'⇒K?" diamond is removed; the figure now shows two parallel constructibility paths from the candidate constitution (with-`ConstitutionalDelta` dashed-bordered node carrying `proofTerm:K'⇒K` → `enactAmendment` → fill-shaded `Enacted` allow terminal; no-witness dotted-bordered node → `rejectAmendment` → double-bordered `Rejected` deny terminal). Caption updated to state "the split is structural, not a runtime decision: the no-delta enact path does not type-check," matching §4 pass-4 framing. Build clean: 13 pages, 0 "!" errors, 0 undefined citations, 0 BibTeX warnings. `v1.tex` re-synced byte-identical. All 9 P1 findings are now `[x]`.
- 2026-05-17 — Step-7 review-swarm agent 2 commissioned: findings in [post-execution-review-2.md](post-execution-review-2.md). 8 new issues: unreferenced floats (P2-I1, major), Compound 289 figure / verb (P2-I2, minor), `bbsDraft`/`dsse`/multiple 2026-mismatched-year bib entries (P2-I3, minor), Table 2 BBS-vs-selective-disclosure terminology (P2-I4, nit), §4 ladder-stability 96pt equation overfull + `treaty.rs:681` mid-break (P2-I5, minor), missing colluding-cosigners adversary class (P2-I6, major), `CHIO_SOURCE` reproducibility gap (P2-I7, minor), abstract three-party / three-vendor toggle (P2-I8, nit). All 8 added to tracker for execution in subsequent cron fires.
- 2026-05-17 — Batched P2 fixes landed (I1/I2/I4/I5/I7/I8): six float references added inline (fig:admission, fig:treaty, fig:amendment, tab:artifacts, tab:theorems, tab:evaluation); Compound 289 verb qualified with "before later rescission by negotiated settlement" + bib `howpublished` cleaned to URL + concise note; Table 2 row renamed to "Selective-disclosure (BBS)"; §4 ladder-stability equation wrapped in `\resizebox{\linewidth}{!}{$\displaystyle ...$}` (96pt overfull cleared); §6 paragraph rewritten with `papers/programmable-sovereignty/bench/` path + `CHIO_SOURCE` parenthetical + Table 2 ref; abstract "three-party" → "three-vendor". Page-count regression to 14 caught and clawed back by collapsing verbose `howpublished`/`note` fields in `zksyncSecurityCouncil2025` and `compoundProposal289` bib entries. Final state: 13 pages, 0 "!" errors, 0 undefined citations, 0 BibTeX warnings, 12 minor overfull boxes remaining (all < 25pt). `v1.tex` re-synced byte-identical. P2-I3 (bib year/author audit) and P2-I6 (threat-model addition) deferred to next fire — heavier prose work.
- 2026-05-17 — P2-I3 + P2-I6 landed. P2-I3: `bbsDraft` authors corrected to `Looker, Tobias and Kalos, Vasilis and Whitehead, Andrew and Lodder, Mike` (verified against IETF datatracker by a primary-source-lookup agent; prior `Victoria Kalos / Mike Whitehead / Ethan Bastian` triple was fabricated; `year={2026}` actually correct per draft-10 2026-01-08); `dsse` corrected to v1.0.2 / `year={2024}`. Other 2026-on-living-doc entries left as accessed-year by convention. P2-I6: §4 threat-model gained the colluding-cosigners sentence ("two kernels operated by a single actor satisfy two-key DSSE but not party-independence"); §9 Table 4 `Verifier-owned stores` row extended in-place to cover bilateral-cosigner distinctness in both Used-for and Residual-risk columns (chose row-extension over new row after a transient page-count regression to 14, clawed back by inlining the addition). Final state: 13 pages, 0 "!" errors, 0 undefined citations, 0 BibTeX warnings. `v1.tex` re-synced byte-identical. All 8 P2 findings now `[x]`.
- 2026-05-17 — User-flagged voice fix: §7 discussion opener ("The v1.1 Chiodos concept note retired the 'agent nation-state' frame...") deleted entirely. The paragraph leaked engineering-meta / project-internal status-report voice (referencing an internal versioned document by name and recounting which framings were dropped during project iteration). Section now opens directly with what the construction supplies, no meta-decision narration. Same voice-defect class as the earlier "live Chiodos branch" / "125 Rust crates" strips. Build clean: 13 pages, 0 "!" errors, 0 undefined citations, 0 BibTeX warnings. `v1.tex` re-synced byte-identical.
- 2026-05-17 — Step-7 review-swarm agent 3 commissioned with explicit instruction to sweep for engineering-meta voice. Findings in [post-execution-review-3.md](post-execution-review-3.md): 8 issues, 4 of them in the engineering-meta voice class (P3-I1 §7 opener still meta, P3-I2 §3 "live implementation sets", P3-I3 §6 "codebase contains" / "paper build loop", P3-I4 §6 replay-paragraph CI jargon), plus 4 others (P3-I5 v1/v2 framing, P3-I6 §1 "workspace proof root", P3-I7 six 2026-year-on-older-artifact bib entries, P3-I8 line-anchor drift treaty.rs:264 vs :455 for same theorem).
- 2026-05-17 — Batched P3 voice fixes landed (I1/I2/I3/I4/I5/I6/I8): §7 opener now a direct property statement ("Chio instantiates receipt-bounded polities..."), §3 "protocol pins" instead of "live implementation sets", §6 jargon swapped for neutral systems-paper language ("Receipt-shaped benchmark scaffolds exist...", "fixtures distributed with the runtime", "stored golden manifest", "canonical verdicts"), v1/v2 framing replaced across §3/§4/§9 with "present construction" / "future work" / "future substrate primitive", §1 "workspace proof root" deleted, Table 2 line anchor aligned with §4 prose. P3-I7 (bib year audit on 6 living-doc entries) deferred — same stylistic call as P2-I3. Build clean: 13 pages, 0 "!" errors, 0 undefined citations, 0 BibTeX warnings. `v1.tex` re-synced byte-identical.
- 2026-05-17 — P3-I7 bib year audit completed: seven `@misc` entries corrected from `year={2026}` to first-publish years — `fuchsiaCapabilities` (2020, Fuchsia CFv2), `projectEverest` (2017), `sigstoreSecurity` (2021), `rekorGithub` (2021), `slsa` (2023, v1.0), `compoundGov` (2020, Governor Bravo upgrade), `polkadotOpenGov` (2023, Gov2 launch). Title clarifications added for `slsa` ("v1.0") and `compoundGov` ("Governor Bravo"). Build clean: 13 pages, 0 "!" errors, 0 undefined citations, 0 BibTeX warnings. `v1.tex` re-synced byte-identical. All 8 P3 findings now `[x]`.
- 2026-05-17 — **Execution complete.** Step-7 review-swarm agent 4 commissioned: zero new findings, verdict "the paper is ready to ship" (see [post-execution-review-4.md](post-execution-review-4.md)). Per procedure step 8, `execution-complete.md` memo written and `CronDelete` called on job id `3bf58ff0` — autonomous 30-min cron terminated. Final paper state: 13 pages, 0 errors, 0 undefined citations, 0 BibTeX warnings, 70 bib entries, 130 Lean theorems (from 113 baseline; +17 across V1/V3/V4/V5/BilateralAccept), `paper.tex` and `v1.tex` byte-identical. Items still needing human action: M2 title decision, Walch send, Pre-Slack outreach, Anthropic co-authorship, V2/V6/V7/V8 engineering, short-paper drafting.
