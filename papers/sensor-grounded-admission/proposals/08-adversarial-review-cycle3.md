# Adversarial Review (Cycle 3 closeout)

Cycle-3 RESEARCH (`final-validation-pass.md`) listed 11 findings (1 top / 2 major / 6 minor / 3 nit). I confirmed the paper state against `paper.tex`, `sections/*.tex`, `lean/SensorGroundedAdmission.lean`, `lean/STATUS.md`, `lean/build-log.md`, `theorems.lean`, `README.md`, `bib.bib`, and the parent paper's §9 limitations. Result: 10 confirmed, 1 refuted (already fixed), 1 new finding added.

## R-3 findings confirmed

**M1 (top finding). N1 amendment disambiguation never added.** Verified. §8:23 still reads `amendment_admissible_iff_backward_refinement` as "gains a paired re-attestation obligation, as Section~\ref{sec:model} establishes." §3:15 says "an amendment that removes a provider must carry a backward-refinement witness over the predicate, as in the parent paper's amendment cycle." §4:55-56 describes the structural improvement. §5:10 calls the required-set block "amends only through the backward-refinement witness and re-attestation obligation." Nowhere in the paper does the prose disambiguate whether the entire sensor-attested construction is itself an amendment of the parent's body-only constitution (in which case introducing a required-set conjunct strictly narrows admission and the witness reduction is trivial but should be named) or a fresh constitution (in which case `amendment_admissible_iff_backward_refinement` applies only to amendments within the new constitution and the §8:23 framing is the wrong tense). Cycle-2 priority 3 prescribed exactly one sentence; the deletion of §5's wire-compat paragraph removed the visible inconsistency but not the underlying ambiguity. A reviewer who knows the parent's amendment ledger reads §8:23 and asks "where is the backward-refinement witness for introducing the required-set conjunct?" Sharpest remaining cycle-3 hit.

**M2. Cosignature party-independence inheritance unacknowledged.** Verified. Parent §9 row 20 carries "two-key DSSE under a single actor collapses to one-of-one (kernel-independence attestation is left as a future substrate primitive)." §3:27 and §4:53 both invoke cosigner attestations under quorum-required admission ("Quorum-required admission additionally requires that every cosigner's attestation cover the required set; a degraded cosigner cannot satisfy quorum even if its receipt body matches"). §9 has no row for cosignature party-independence, and §7:11's "strictly tighter than the parent paper's predicate alone" treats cosignatures symmetrically. A heterogeneous bilateral treaty between a TEE-rooted polity and a single-key polity inherits the weakest-cosigner strength; the paper invokes the primitive without acknowledging the parent's documented residual. Cycle-2 S-C2-2 (heterogeneous-trust cosignature) is the same finding from the systems-reviewer angle and remains downstream.

**Min3. Old Theorem 1 name in companion docs.** Verified directly. `lean/STATUS.md:15` ("## Theorem 1: `admission_under_degraded_state_distinguishable_from_healthy`"), `lean/build-log.md:88` (axiom-print output), `theorems.lean:229` (the entire `:= sorry`-stubbed draft), and `README.md:17` all carry the old name. Publication-targeted scope (`paper.tex`, `sections/*.tex`, `lean/SensorGroundedAdmission.lean`) is clean. If the `lean/` directory ships as supplementary material (USENIX norm), STATUS.md's section title contradicts its own body discussion, and `theorems.lean` at paper root is a stale `:= sorry` draft. Mechanical to fix; one-line in each file.

**Min4. Theorem 4's `_h_prev_decl` inert binder undisclosed in §9:28.** Verified. STATUS.md:126 explicitly notes "The `_h_prev_decl` binder remains underscore-prefixed (it constrains how `declprev` was obtained but does not appear in the conclusion's structural-improvement claim)." §9:28 mentions only Theorem 3's `destructiveAdmissionFamily` inert binder. Cycle-2 F-C2-2 is half-closed: STATUS is honest, the paper discloses one of two inert binders. The §4:56 paragraph also fails to call out the second one.

**Min5. Schema-evolution inheritance unacknowledged.** Verified. Parent §9 line 44 carries "Schema evolution across vendor kernels. Two kernels at different schema versions exchanging receipts currently deny on `unsupported_treaty_scope_schema` without distinguishing the failure from a constitutional denial in operator dashboards; a versioned-predicate compatibility profile and a distinct `schema_mismatch` denial code are required for multi-tenant deployment." The sensor-grounded paper adds new wire fields (provider records with eleven fields, clock records with four fields, required-set declarations) and deleted the local wire-compat paragraph in cycle 3, leaving no schema-evolution row at all. The inheritance dropped silently.

**Min6. Theorem-inventory discipline parent row unacknowledged.** Verified. Parent §9 row 22 carries "Lean theorem inventory is maintained with code changes... Stale inventory can make proof coverage look broader than it is." Sensor-grounded §9 has no parallel row. The paper relies on the inventory discipline to make its four-theorem claim load-bearing (Lean names map to mechanized claims) but does not name the inherited residual. STATUS.md is the local audit anchor but is not referenced from §9.

**Min7. `intelTDXSpec2023` half-covers §3:21 PCK/QE signing claim.** Verified. §3:21 states "Intel TDX Quoting Enclave keys are signed by a Provisioning Certification Key derived from fused secrets." `bib.bib:254` describes the cite as "TDX Module Base Architecture Specification" doc 348549-002US, which covers TDREPORT generation inside the TD module. The QE/PCK signing chain belongs to Intel's Quoting Service / DCAP documents (a separate publication). The cite supports half the claim. AMD-SNP cite (Pub 56860 r1.58) and Apple SEP/UIK cite (platform-security guide) are correct; only the TDX cite half-covers.

**Nit8. §6:18 ES synthetic-counts contradiction.** Verified. §6:18 reads: "The macOS `EndpointSecurity` system extension shipped with the substrate does not call `es_new_client` or `es_subscribe`; its event source is an in-memory recorder. The host-snapshot path's ES provider record is real-but-stubbed: runtime flags, install state, drop and miss counts, and full-disk-access signal are present, but the upstream sensor that would populate them under production is absent." On first scan, "does not call `es_new_client`" plus "drop and miss counts ... are present" reads as contradiction. The resolution (synthetic in-memory recorder) is in the same paragraph but the reader pays a cognitive tax. One word ("synthetic" before "drop and miss counts") would close the nit.

**Nit10. §1:12 future-work pointer to §10 resolves obliquely.** Verified. §1:12 says "the auditor is the structural complement of this work and is named as future work in Section~\ref{sec:conclusion}." §10:8 names the auditor but §10:10 (the "natural continuation" sentence) lists per-tenant required sets, time-windowed thresholds, and behavioral attestation without explicit auditor mention. The auditor is named in §10:8 prose but does not appear in the bullet list of future work directions. Resolved but not tight.

## R-3 findings refuted / overstated

**Nit9 refuted. §10:6 preserves "three-conjunct shape."** R-3 RESEARCH listed this nit ("§10:6 drops 'three-conjunct shape' qualifier present in §1:19"), but the actual §10:6 sentence reads: "extracts required-set coverage from the admission predicate's three-conjunct shape; any admitted receipt, viewed through that shape, has a declared required set and an attestation covering it." The qualifier is present. Per the task brief, cycle-3 WRITE-B added it back. Already fixed; no action needed.

## Findings R-3 missed

**New finding NF1. `theorems.lean` at paper root is a packaging hazard, not just companion-doc drift.** R-3 minor 5 names this file as carrying the old theorem name. But the deeper issue: `theorems.lean` is a 298-line draft with all four theorems stubbed `:= sorry` (lines 243, 257, 274, 296) sitting in the paper root, while `lean/SensorGroundedAdmission.lean` is the real proven file 23kB / 583 lines below it. A reviewer who downloads the paper directory and sees a 298-line `theorems.lean` with `sorry` next to a 583-line `SensorGroundedAdmission.lean` without `sorry` reasonably asks "which one is the paper's actual mechanization?" The README.md:51 reference to `theorems.lean` as "the Lean statements with `:= sorry` placeholders" predates the proven file's existence. Either delete `theorems.lean`, mark it as obsolete in a one-line file header, or update README.md to point at the real file. R-3 framed this as theorem-name drift; the larger issue is two Lean files with the same theorem names (one proven, one stubbed) in the same paper directory, which makes the supplementary-material story incoherent. Severity: minor, but mechanical.

**No further substantive findings.** Bib spot checks (5 entries: `hammoudConfidentialCloud2023`, `armCCAToken2026`, `awsNitroNSM`, `applePCC2024`, `yeh1996Boeing777`) match the surrounding §2:21 / §8:11 / §8:16 claims to within editorial tolerance. The TDX cite gap R-3 already named is the only half-cover. No fabricated authors or years detected. §4 mathematical statements (the headline existence theorem, the partition-contingency biconditional, the destructive-admission projection, the amendment-improvement theorem) all match the Lean theorem statements at SensorGroundedAdmission.lean:352, :421, :467, :546. No voice leaks survived the cycle-3 polish: function-symbol citations (`endpoint_sensor_state_from_macos_host`, `endpoint_sensor_state_content_hash`) are still in §5:18 and §6:14 inside `\codepath{}`, which is the paper's documented style for runtime symbols and not a leak.

## Termination call: ready with one substantive fix

**The paper is ready for termination if cycle-3 FIX closes M1, M2, Min3 (rename propagation), and Min4 (T4 binder one-line disclosure). The other five minor / nit items can be deferred to camera-ready without surfacing in cycle 4.**

The two majors are the only items that a careful reviewer can use to write "this paper has structural debt at the inheritance boundary with the parent." Both are one-sentence fixes. M1 is the disambiguation sentence cycle-2 priority 3 already prescribed (no new text generation needed; the sentence is in proposal 06). M2 is one §9 row in three-column format, with text directly importable from parent §9 row 20.

Min3 is mechanical: four files, one-line change each (STATUS.md, build-log.md, theorems.lean if kept, README.md). Min4 is a half-sentence addition to §9:28: "and Theorem 4's `_h_prev_decl` binder is similarly inert." NF1 is a one-line README.md update or a five-line stale-banner in theorems.lean.

The remaining minor / nit items (Min5 schema evolution, Min6 inventory discipline, Min7 TDX cite, Nit8 ES contradiction, Nit10 §1 pointer) are real but each survives in published USENIX Security papers regularly. None is a 30-second reviewer flag; none rises above editorial polish. If the FIX cycle has bandwidth, Min5 (one §9 row) and Min7 (add DCAP cite or hedge the §3:21 claim to "in the broader TDX attestation flow") are the highest-marginal-value of this group.

**No hidden cycle-4 gotcha.** I looked for a §4 mathematical error, a fabricated bib entry, a voice leak that survived, and a citation that doesn't support its claim. None found. The Lean compiles, the four theorems match their paper-prose statements, the §6 arithmetic (6+13=19) is internally consistent, and the §8 wire-format survey is anchored to primary sources. The remaining residuals are documented limitations and inheritance-boundary acknowledgments, not silent overclaims.

## Top-3 priority items for the (hopefully final) FIX cycle

1. **Add the N1 disambiguation sentence.** Insert one sentence to §3 (preferred) or §9 (acceptable): "The sensor-attestation construction is a fresh constitution, not an amendment of the parent paper's body-only constitution; the backward-refinement obligation in `amendment_admissible_iff_backward_refinement` applies to amendments within the new constitution, not to its introduction." Also reword §8:23 to remove "gains a paired re-attestation obligation" phrasing. Closes M1.

2. **Add the cosignature party-independence §9 row.** One three-column row: "Cosigner party-independence (inherited from parent §9). | Quorum-required admission with multiple cosigners. | Two-key DSSE under a single actor collapses to one-of-one; a heterogeneous bilateral treaty between a TEE-rooted polity and a single-key polity inherits the weakest-cosigner strength. Kernel-independence attestation is a future substrate primitive in the parent assumption ledger." Closes M2.

3. **Propagate the Theorem 1 rename + Theorem 4 binder disclosure.** Four-file mechanical pass: `STATUS.md:15`, `build-log.md:88`, `README.md:17`, and either delete `theorems.lean` or replace its header with a one-line "this file is obsolete; see `lean/SensorGroundedAdmission.lean`" banner. Append to §9:28: "Theorem 4's `_h_prev_decl` binder is similarly inert and retained for the same structural-hook reason." Closes Min3, Min4, NF1.

After items 1, 2, 3, the paper is genuinely ready for termination. Items 4-9 are camera-ready polish.

---

Count: 10 confirmed, 1 refuted, 1 new. Termination call: ready after one FIX cycle landing the three items above.
