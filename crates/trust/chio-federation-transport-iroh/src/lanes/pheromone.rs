//! Lane a: pheromone directed batches over a direct per-peer QUIC stream.
//! ADAPTER-SPEC section 4 row (a). Migration of the shipped chio-pheromone-relay
//! HTTP transport; reuses its SQLite outbox/inbox for store-and-forward.
//!
// TODO(phase 2, lane a): implement.

/// Reserved for lane a. Present so the module tree compiles while the lane team builds.
pub fn reserved() {}
