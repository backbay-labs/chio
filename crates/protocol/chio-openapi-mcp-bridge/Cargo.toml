[package]
name = "chio-openapi-mcp-bridge"
description = "Bridge that exposes Chio-governed HTTP APIs as MCP tool surfaces via OpenAPI spec"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
name = "chio_openapi_mcp_bridge"

[features]
default = []
# Enables `chio_openapi_mcp_bridge::fuzz`, the libFuzzer entry-point
# module. Off by default; turned on only by the `chio-fuzz` standalone
# workspace at `../../fuzz`.
fuzz = ["dep:arbitrary"]

[dependencies]
async-trait = { workspace = true }
chio-core = { package = "chio-core-types", path = "../../core/chio-core-types" }
chio-egress-contract = { workspace = true }
chio-kernel = { workspace = true }
chio-manifest = { workspace = true }
chio-mcp-edge = { workspace = true }
chio-openapi = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
arbitrary = { version = "1", features = ["derive"], optional = true }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
