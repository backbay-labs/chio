[package]
name = "chio-log-redact"
description = "Tracing log redaction layer and macro for Chio operator surfaces"
version.workspace = true
edition.workspace = true
# Kani 0.67 uses Rust 1.93; this formal-proof dependency remains compatible.
rust-version = "1.93"
license.workspace = true
repository.workspace = true
publish = false

[lib]
name = "chio_log_redact"

[dependencies]
chio-data-guards-redactors-default = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true, features = ["registry"] }

[dev-dependencies]
tracing-subscriber = { workspace = true, features = ["registry"] }

[lints]
workspace = true
