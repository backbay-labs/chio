--- sdks/typescript/packages/node-http/test/sidecar-client.test.ts	2026-09-09 23:14:46
+++ /tmp/chio-kernel-release-audit-20260909/sdk-gate-repair/node-http-diagnosis/package/test/sidecar-client.test.ts	2026-09-09 23:47:17
@@ -220,7 +220,7 @@
 
   expect(caught).toBeInstanceOf(SidecarError);
   const sidecarError = caught as SidecarError;
-  expect(sidecarError.code).toBe(code);
+  expect(sidecarError.code, `${sidecarError.message}; actual HTTP ${sidecarError.statusCode}; expected HTTP ${statusCode}`).toBe(code);
   expect(sidecarError.statusCode).toBe(statusCode);
 }
 
