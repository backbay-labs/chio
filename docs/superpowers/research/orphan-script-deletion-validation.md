# Orphan / Near-Orphan Script Deletion - Adversarial Validation Appendix

Scope: validate the planned migration step "delete confirmed-orphan scripts and
consolidate near-orphans" in the Chio repo
(`/Users/connor/Medica/backbay/standalone/arc`). Read-only. Every basename was
re-grepped across the entire working tree (with and without `.sh`, and with the
`check-` / `measure_` prefix stripped) excluding only noise trees that are not
part of the source of truth: `.git/`, `**/node_modules/**`, `.worktrees/**`,
`.claude/worktrees/**`, `**/target/**`, `_apalache-out/`, `coverage/`.

Tooling: ripgrep 15.1.0. House rule honored: no em dashes.

## Method note on the noise trees

`find` showed every candidate basename is duplicated under `.worktrees/research/*`
and `.claude/worktrees/charming-feistel-8595da/` (sibling worktrees, not the live
tree) and `build-ios-framework.sh` also appears under
`sdks/typescript/node_modules/react-native/sdks/hermes-engine/utils/` (an
unrelated vendored React Native helper with the same name). These are NOT
references to the repo scripts and were excluded. The vendored react-native copy
is a name collision only; it has no bearing on `scripts/build-ios-framework.sh`.

All 14 candidate scripts are git-tracked (`git ls-files` returns all 14).

## 1. ORPHANS - confirmed zero operational references (SAFE-DELETE)

For each, the ONLY hits anywhere in the live tree are inside the two research
documents in this very directory (`docs/superpowers/research/scripts-audit.md`
and `xtask-cicd-tool-design.md`), which are descriptive prose / a prior audit and
a proposed-future xtask design. Neither is operational wiring. Zero references in
`.github/`, `Makefile`, `xtask/`, any other `scripts/*.sh`, `sdks/`, `examples/`,
`audits/`, `compliance/`, `formal/`, or any `*.toml`.

| Script | Operational refs | Doc-only refs | Verdict |
| --- | --- | --- | --- |
| `scripts/check-adversarial-threat-link.sh` | none | scripts-audit.md:112; xtask-cicd-tool-design.md:237 | SAFE-DELETE |
| `scripts/check-chio-attest-buyer-fixtures.sh` | none | scripts-audit.md:113 | SAFE-DELETE |
| `scripts/check-docker-deployable-experience.sh` | none | scripts-audit.md:114 | SAFE-DELETE |
| `scripts/check-framework-integration-examples.sh` | none | scripts-audit.md:115 | SAFE-DELETE |
| `scripts/check-tool-server-async.sh` | none | scripts-audit.md:116; xtask-cicd-tool-design.md:232 | SAFE-DELETE |
| `scripts/measure_chio_core_rebuild.sh` | none | scripts-audit.md:117 | SAFE-DELETE |

Partial-name searches (`adversarial-threat-link`, `attest-buyer-fixtures`,
`docker-deployable`, `framework-integration-examples`, `tool-server-async`,
`core_rebuild`) returned no additional hits outside the candidate scripts
themselves. These six are safe to delete.

Stale-doc note (non-blocking, not a wiring break): deleting them leaves dangling
mentions in `scripts-audit.md` and `xtask-cicd-tool-design.md`. Those are this
research dir's own artifacts; update or leave as historical record - either way
nothing executes them.

## 2. NEAR-ORPHANS

None of the eight near-orphans is referenced by any workflow under `.github/`,
by `Makefile`, by `xtask/`, or by any other script (no script-to-script
invocation found). `scripts/ci-workspace.sh` does not call any of them.
`reproducible-build.yml` does NOT call `rebuild-from-source.sh` (it does its own
inline SHA256SUMS diff).

### 2a. SAFE-DELETE near-orphans (self-only)

| Script | Refs found | Verdict |
| --- | --- | --- |
| `scripts/kani-changed-harnesses.sh` | docs only (scripts-audit.md:52,122,174,233). Not called by `run-kani-manifest.sh` or any workflow. | SAFE-DELETE |
| `scripts/rebuild-from-source.sh` | docs only (scripts-audit.md:38,125; xtask-cicd-tool-design.md:286). `reproducible-build.yml` does not invoke it. | SAFE-DELETE |

### 2b. KEEP-WIRED-TO-DOC-CONTRACT (do NOT silently delete - fail-closed risk)

These have no CI wiring but ARE the enforcement mechanism named by living
evidence / formal / compliance prose. Deleting them silently breaks a documented
fail-closed contract (the doc tells a human or a future gate to run the script;
the script would be gone). Treat as blockers for blind deletion - they need an
owner decision (retire the doc claim too, or keep the script).

| Script | Binding references (file:line) |
| --- | --- |
| `scripts/check-mapping.sh` | `formal/MAPPING.md`:7,28,132,136 (self-described as "enforced by"); `formal/issue-templates/property-counterexample.md`:12,93,121; `compliance/hitrust/narratives/formal-evidence-bridge.md`:43 |
| `scripts/triage-threat-rows.sh` | `audits/evidence/threat-row-triage/per-row-triage.md`:5,50 ("Generated by ... Run `bash scripts/triage-threat-rows.sh`") |
| `scripts/check-corpus-metadata.sh` | `fuzz/corpus_metadata.toml`:4,851 (comments stating the file is "Validated by scripts/check-corpus-metadata.sh"). Not executed by CI, but the data contract names it. |

### 2c. KEEP-RELOCATE (referenced only by SDK readme / manifest - move near its SDK)

These are mobile/SDK build helpers referenced ONLY from SDK docs and one Swift
manifest comment. They are not CI gates. They may be relocated into their SDK
tree, but every reference must be updated in the same change or the doc/manifest
path breaks.

| Script | References to update on relocation |
| --- | --- |
| `scripts/build-android-aar.sh` | `sdks/jvm/chio-kernel-mobile/README.md`:18 (`bash scripts/build-android-aar.sh`) |
| `scripts/build-ios-framework.sh` | `sdks/swift/README.md`:11; `sdks/swift/Package.swift`:6 (comment) |
| `scripts/check-sdk-publication-examples.sh` | `sdks/typescript/chio-ts/README.md`:63; `sdks/python/chio-py/README.md`:62; `sdks/python/chio-py/RELEASING.md`:12 |

Relocation targets confirmed to exist: `sdks/jvm/chio-kernel-mobile/`,
`sdks/swift/` (has `Frameworks/`, `Package.swift`, `README.md`, `Sources/`,
`Tests/`), `sdks/python/chio-py/`, `sdks/typescript/chio-ts/`.

## 3. scripts/__pycache__ - confirmed gitignored + untracked (SAFE)

- Contents: 5 `*.pyc` (compiled `check-architecture-docs`, `check-review-slices`,
  `check-rust-file-hygiene`, `check-rust-public-surface`, `check-stub-surfaces`).
- `git ls-files scripts/__pycache__/` -> empty (untracked).
- `git check-ignore -v scripts/__pycache__/check-architecture-docs.cpython-314.pyc`
  -> `.gitignore:55: __pycache__/`. Also `.gitignore:130: *.pyc`.
- `git status --porcelain scripts/__pycache__/` -> empty (ignored, not surfaced).
- It is regenerated build cache. Safe to `rm -rf` locally; nothing in the repo
  references it.

## 4. scripts/tests/*.test.sh wiring - prior-audit claim CORRECTED

25 meta-test files exist (24 `*.test.sh` + 1 `promote_fuzz_seed_adversarial.bats`).

- 15 distinct `*.test.sh` are referenced by workflows under `.github/`:
  13 in `ci.yml` (lines 80, 85, 102, 110-119) and 2 in
  `threat-model-coverage.yml` (lines 40-41 paths-filter, 82, 85 run).
- 12 are also listed by the runner `scripts/ci-workspace.sh` (lines 10, 27,
  29-38) - an explicit hardcoded list, NOT a glob. `ci-workspace.sh` is reached
  via `scripts/qualify-release.sh -> scripts/ci-workspace.sh`
  (per `release-qualification.yml`:15 comment).
- The workflow-referenced set (15) is the union; the `ci-workspace.sh` set (12)
  is a subset. `check-http-egress-contract`, `check-threat-coverage`,
  `check-threat-coverage-mutants` are workflow-only (not in ci-workspace.sh).

ADVERSARIAL CORRECTION: `scripts-audit.md`:87,188 claims "10 are wired into CI
... the rest run via a test runner." Both numbers are wrong. (a) 15 are wired,
not 10. (b) There is NO test runner that globs `scripts/tests/`. I searched for
`for ... scripts/tests`, `find scripts/tests`, `scripts/tests/*`, `tests/*.test`
across the whole tree - zero dynamic runner. The 10 unreferenced files below are
genuinely NOT invoked by any workflow, by ci-workspace.sh, or by any glob loop:

```
check-cargo-vet-exemptions.test.sh
check-sidecar-docker-context.test.sh
ci-release-assurance.test.sh
fuzz-budget-hard-halt.test.sh
install-apalache-checksum.test.sh
mutants-gate-missing-outcomes.test.sh
mutation-aggregate-pending-rerun.test.sh
promote_fuzz_seed_adversarial.bats
run-kani-manifest-empty-match.test.sh
tuf-rebake-workflow.test.sh
```

This is OUT OF SCOPE for the current deletion step (these files are not on the
candidate list) and they are NOT proposed for deletion here. Flagging only so the
"covered by a test runner" assumption is not carried into a later step as a
safety claim. If a later step wants to retire any of these 10, it must first
either wire them into a runner or accept they are dead.

## 5. Final SAFE-DELETE list (zero operational references)

```
scripts/check-adversarial-threat-link.sh
scripts/check-chio-attest-buyer-fixtures.sh
scripts/check-docker-deployable-experience.sh
scripts/check-framework-integration-examples.sh
scripts/check-tool-server-async.sh
scripts/measure_chio_core_rebuild.sh
scripts/kani-changed-harnesses.sh
scripts/rebuild-from-source.sh
```

Plus optional housekeeping: `rm -rf scripts/__pycache__` (gitignored cache).

## 6. Items that must NOT be blindly deleted (blockers)

- `scripts/check-mapping.sh` - enforcement named by `formal/MAPPING.md`,
  `formal/issue-templates/property-counterexample.md`, and a HITRUST compliance
  narrative. Owner decision required.
- `scripts/triage-threat-rows.sh` - generator named by audit evidence
  (`audits/evidence/threat-row-triage/per-row-triage.md`).
- `scripts/check-corpus-metadata.sh` - validator named in the
  `fuzz/corpus_metadata.toml` data contract.
- `scripts/build-android-aar.sh`, `scripts/build-ios-framework.sh`,
  `scripts/check-sdk-publication-examples.sh` - KEEP-RELOCATE; references in SDK
  README / RELEASING / Package.swift must be updated atomically with any move.
