# PR #966 -> main : Merge Resolution Plan (research/brainstorm output)

Base is `main` (05770cf59), 222 commits diverged from merge-base b736f5bd0. 11 conflicts (7 non-trivial money-path + 4 trivial).

## Overall feasibility

"BOUNDED, careful, self-contained merge - NO upstream or author input required. All 7 per-file analyses are internally consistent and mutually reinforcing: the same four cross-file themes (3-arg signature, fail-closed wrapper, budget_lease_acquired refcount, dispatch_started split) recur predictably, and the working-tree spot-checks I ran confirm the analyses exactly (MERGE_HEAD=05770cf59; the three ripple 2-arg calls at kernel_drop_guard.rs:179, nested_flow_evaluation.rs:807-810, async_evaluation_core.rs:899-902 are present as described; reconciliation.rs:275 is already correctly 3-arg and needs no edit; the deny_metadata locals are auto-merged from theirs). The resolution is a systematic UNION - ours' money semantics folded onto theirs' safety scaffold - not a design negotiation. The genuine difficulty is not conceptual but mechanical discipline: roughly 6 mandatory edits OUTSIDE conflict markers (3 arity ripples + iroh_background cfg-gate + the two drop-guard gate broadenings + the shell lib port) that git did NOT flag and that a naive markers-only pass will miss, plus 3 edits that must thread budget_lease_acquired into ours-only code (ReserveForCaller literal, reserving-response helper, ReverseForRetry call). EFFORT: about 2-4 focused hours for an engineer fluent in this kernel - roughly 1-2h to resolve and apply the 11 files following the ordered plan, 30-60min for the first clean cargo build --workspace (cold cache, several minutes each iteration), and the balance on the dual iroh builds, the named test suites, and the adversarial grep pass. Confidence HIGH across all 7 files. Highest-attention items: kernel_drop_guard.rs edits B/C (silent P1 leak if skipped), the reserving-response conditional release (silent oversubscription if wrong), and building BOTH iroh feature settings. This should be done by one engineer in a single sitting to keep the cross-file signature/refcount decisions coherent, then run through code-review/security-review before committing."


## Cross-file themes

- THEME A - budget_execution_receipt_metadata signature widening (OURS, commit 39ad63246): ours added a 3rd param execution_nonce_id: Option<&str>; the single merged definition lives at crates/kernel/chio-kernel/src/kernel/validation.rs:756. This makes the merged def 3-arg and forces EVERY caller to pass a 3rd arg. Caller files: validation.rs (1073 clean, 1451/1473 clean, plus the conflicted incomplete arm ~1488), reconciliation.rs:275 (already 3-arg with Some(nonce) - clean, no edit), kernel_drop_guard.rs (179 auto-merged 2-arg -> NEEDS None; 425 in resolved body), evaluation_helpers.rs (231/277/362 - merged defs feed None), nested_flow_evaluation.rs (249/302/346 clean; 691/701/740/750 in conflict regions; 807 auto-merged 2-arg -> NEEDS None), async_evaluation_core.rs (324/383/434 clean; 782/792/831/841/920 in conflict regions; 899 auto-merged 2-arg inside deny_metadata -> NEEDS None). On all reversal/abort paths the 3rd arg is None (no authoritative nonce); only authoritative-spend/reconcile sites pass Some(nonce_id).
- THEME B - main's fail-closed wrapper mark_runtime_admission_reservations_retained_fail_closed(metadata)->Option<Value> (THEIRS, commit c201afbd0/84e98b9d0; defined crates/kernel/chio-kernel/src/kernel/dispatch.rs:431). Copies reserved_* runtime-reservation ids into retained_* so operators can recover a burned destructive-lease / treaty / swarm continuation from the signed receipt. Wraps abort/incomplete receipt metadata in: async_evaluation_core.rs (cancelled 777-796, incomplete 826-845, side-effect deny via deny_metadata local 895-909), nested_flow_evaluation.rs (cancelled 686-709, incomplete 735-758, deny via deny_metadata ~814), validation.rs (incomplete-stream finalize arm ~1488, plus the ours-only 2nd incomplete arm ~1091), kernel_drop_guard.rs (post-dispatch drop tail, produces receipt_metadata local). Orthogonal to Theme A: it wraps the OUTER expression while Theme A widens the INNER call.
- THEME C - sibling-sum child-budget LEASE refcount budget_lease_acquired: bool (THEIRS, commits ad64e8f40/c85c47036/818d1c5c9). admit_capability_budget returns it; it is threaded through the preflight/cleanup builders (evaluation_helpers.rs: build_execution_nonce_preflight_allow_response_after_cleanup grows to 7-arg), added as a field on the PreDispatchCleanupDeny struct, and added as the 8th arg to PostAdmissionDropGuard::new. Cleanup releases a child-budget lease only if this evaluation acquired one (refcounted release; prevents double-free of an overlapping sibling's share). Files: evaluation_helpers.rs (struct + builders), async_evaluation_core.rs (captured line 462; region-1 preflight-allow call + ReserveForCaller PreDispatchCleanupDeny literal + reserving-response call; guard new() at 628), nested_flow_evaluation.rs (def line 374; guard new() at 492), kernel_drop_guard.rs (struct field + gated release in handle_pre_dispatch_drop step 4).
- THEME D - dispatch_started drop-guard restructure (THEIRS, commits c2e8be7e3/a6d26dbc4 et al). PostAdmissionDropGuard gains dispatch_started + child_receipts Vec; Drop::drop splits into handle_pre_dispatch_drop() (5-step full unwind + PreDispatchCleanupFault receipt) vs post-dispatch (flush_buffered_child_receipts_from_drop + unwind_charge_from_drop + fail-closed wrap -> receipt_metadata). POST_ADMISSION_DROP_REASON broadened from '...after monetary admission' to '...after admission'. Files: kernel_drop_guard.rs (primary), with 8-arg new() callers in async_evaluation_core.rs:628 and nested_flow_evaluation.rs:492 (both already auto-merged to theirs' shape).
- THEME E - OURS PreflightHoldDisposition (ReserveForCaller/ReverseForRetry) MustPrepay prepay/refund + reserved-nonce path (commits dae6e6ee5/ccc350c90/d65e16c51/543336870/67760a581). Adds ensure_reserved_mustprepay_prepaid gate, build_execution_nonce_authorization_reserving_response, refund_reserved_mustprepay_prepayment, and the pending_reversal_marker durable breadcrumb. Files: async_evaluation_core.rs (region 1 match), evaluation_helpers.rs (helpers), kernel_drop_guard.rs (pending_reversal_marker + no-charge MustPrepay leak fix), tests/chio_runtime.rs (regression test), tests/support_monetary.rs (ReverseFailingBudgetStore - auto-merged).
- THEME F - iroh default-off cargo feature gating (OURS, commit 50ab32a6e) vs main's in-tree iroh directory-reloader + liveness watchdog (THEIRS, commit 05770cf59). NOT a money-path theme. Files: chio-cli/.../pheromone/relay.rs (this conflict) and the already-resolved sibling parent pheromone.rs (the pattern to mirror). Also chio-cli Cargo.toml (iroh optional dep).
- THEME G - trusted-keys single-source-of-truth extraction (OURS, commit d3eff6e33) vs main's 6 new web3 contract settlement env vars (THEIRS, 261274f86/f21a58c80). Files: scripts/check-chio-transaction-passport.sh (this conflict), scripts/lib/chio-proof-trusted-keys.sh (the extracted lib - must receive the 6 vars), scripts/demo/flagship-wall-stops-money.sh + scripts/tests/check-chio-transaction-passport.test.sh (consumers, no edit).

## Shared decisions (make once, apply everywhere)

- DECISION 1 (signature): Adopt OURS 3-arg budget_execution_receipt_metadata as the ONE merged definition (validation.rs:756). Every call site is 3-arg. Reversal/abort/incomplete paths pass None as the 3rd arg; only authoritative-spend and reconcile sites pass Some(nonce_id). Consequence: three auto-merged-from-theirs 2-arg calls MUST be patched to add , None -> kernel_drop_guard.rs:179, nested_flow_evaluation.rs:807-810, async_evaluation_core.rs:899-902. reconciliation.rs:275 is already 3-arg; do not touch.
- DECISION 2 (fail-closed wrapper): Adopt THEIRS mark_runtime_admission_reservations_retained_fail_closed on EVERY abort/incomplete/deny receipt-metadata path, wrapping OURS 3-arg inner match. This is additive and transparent (Option<Value>->Option<Value>). Apply in both evaluation files (cancelled+incomplete arms union wrapper+None; deny arms take theirs' precomputed deny_metadata local), validation.rs (both incomplete-stream finalize arms), and kernel_drop_guard.rs (theirs' post-dispatch tail already produces the wrapped receipt_metadata).
- DECISION 3 (lease refcount): Adopt THEIRS budget_lease_acquired refcount as the merged struct/builder shape (7-arg preflight-allow builder, PreDispatchCleanupDeny field, 8-arg guard new). Thread it into OURS-only new code: the ReverseForRetry preflight-allow call (7th arg), the ReserveForCaller PreDispatchCleanupDeny literal (new field), and build_execution_nonce_authorization_reserving_response (new param) so its non-monetary release_admitted_capability_budget becomes CONDITIONAL on budget_lease_acquired (ours releases unconditionally today; that would double-free under theirs' refcount).
- DECISION 4 (drop-guard shape): Adopt THEIRS dispatch_started pre/post split (struct + new() + handle_pre_dispatch_drop/unwind_charge_from_drop) as the merged shape. FOLD ours' money semantics INTO theirs' helpers rather than keeping ours' body: (a) broaden the monetary-unwind gate from charge_result().is_some() to charge_result().is_some() || payment_authorization.is_some() in BOTH unwind_charge_from_drop and handle_pre_dispatch_drop step 1 (P1 no-ceiling MustPrepay leak fix from 67760a581); (b) add ours' pending_reversal_marker Err(reverse) fold and the (Err,None) payment-release warn arm inside unwind_charge_from_drop.
- DECISION 5 (disposition path): Keep OURS PreflightHoldDisposition match (ReverseForRetry/ReserveForCaller) verbatim - theirs has no such concept - subsuming theirs' single 7-arg preflight-allow call under the ReverseForRetry arm. Never take theirs whole-hog on async_evaluation_core.rs region 1 (deletes the MustPrepay money path).
- DECISION 6 (const text): Where POST_ADMISSION_DROP_REASON diverges, adopt THEIRS text '...after admission' (guard now fires for non-monetary admissions) and keep THEIRS PRE_DISPATCH_CLEANUP_FAULT_REASON; UNION in ours' pending_reversal_marker fn + test.
- DECISION 7 (iroh): Union under OURS feature-gate discipline, mirroring the already-resolved parent pheromone.rs. All theirs-new iroh symbols (note_router_liveness, run_directory_reloader, DirectoryReloadConfig) go in the #[cfg(feature=\"iroh\")] import group; theirs' iroh_background abort block gets #[cfg(feature=\"iroh\")]; and the auto-merged (unmarked) iroh_background DEFINITION block must ALSO be cfg-gated.
- DECISION 8 (trusted keys): Accept OURS source-line resolution in the shell script; port THEIRS 6 web3 CHIO_PUBLIC_SETTLEMENT_TRUSTED_* exports into scripts/lib/chio-proof-trusted-keys.sh (between ORACLE_KEYS and ALLOWED_CHAIN_IDS) so the sourced lib carries them for all three consumers.

## Ordered plan

1. STEP 0 - Confirm merge state: MERGE_HEAD = 05770cf59 (origin/main) is in progress; 11 conflicted files remain (7 non-trivial + 4 trivial). Work bottom-up: fix the merged DEFINITIONS/signatures first so callers type-check, then callers, then tests, then unrelated files, then gates.
2. STEP 1 - evaluation_helpers.rs (foundational defs; trivial-union file but load-bearing): resolve so the merged shape is (a) build_execution_nonce_preflight_allow_response_after_cleanup = 7-arg with theirs' budget_lease_acquired param AND ours' 3-arg internal receipt-metadata call (pass None); (b) PreDispatchCleanupDeny struct carries the budget_lease_acquired field; (c) build_execution_nonce_authorization_reserving_response gains a budget_lease_acquired param that GATES its non-monetary release_admitted_capability_budget (release only when true). Verify the 3 receipt-metadata calls at 231/277/362 are 3-arg (362 already shows None,None).
3. STEP 2 - validation.rs: (a) confirm def at line 756 is the 3-arg version (execution_nonce_id: Option<&str>) - it wins; (b) resolve the sole conflict hunk (~1488-1502, Incomplete finalize arm): keep OURS block-form arm and its local `merged`, WRAP it as self.mark_runtime_admission_reservations_retained_fail_closed(merged), rename theirs' merged_extra_metadata->merged, keep theirs' 5-line comment, use ours' block-form closing )}; (c) NON-CONFLICTED behavior-completeness edit: wrap the 2nd (ours-only) incomplete arm at ~1091 with the same helper (compiles either way; do it for parity). 1451/1473 already 3-arg.
4. STEP 3 - async_evaluation_core.rs: REGION 1 (490-578) keep OURS `match preflight_disposition{...}`; append budget_lease_acquired as 7th arg on the ReverseForRetry build_execution_nonce_preflight_allow_response_after_cleanup call; add budget_lease_acquired field to the ReserveForCaller PreDispatchCleanupDeny literal; pass budget_lease_acquired into build_execution_nonce_authorization_reserving_response; drop theirs' single 7-arg call. REGIONS 2 (777-796) & 3 (826-845): union = theirs' mark_...fail_closed wrapper around OURS 3-arg match (None). REGION 4 (915-930): take THEIRS deny_metadata local, drop ours' inline match. NON-CONFLICTED RIPPLE (MANDATORY): patch the deny_metadata block 2-arg call at lines 899-902 to add , None (verified present in working tree). 324/383/434 already 3-arg.
5. STEP 4 - nested_flow_evaluation.rs: REGIONS 1 (686-709 cancelled) & 2 (735-758 incomplete): union = theirs' mark_...fail_closed wrapper + OURS trailing None on the inner call. REGION 3 (823-838 deny): take THEIRS - pass the precomputed deny_metadata binding, discard ours' inline match. NON-CONFLICTED RIPPLE (MANDATORY): patch the hoisted deny_metadata block 2-arg call at lines 807-810 to add , None (verified). 249/302/346 already 3-arg; budget_lease_acquired plumbing (374 etc.) is theirs-only non-conflicted, leave as-is.
6. STEP 5 - kernel_drop_guard.rs: REGION 1 (12-36): union consts - adopt THEIRS POST_ADMISSION_DROP_REASON '...after admission' + THEIRS PRE_DISPATCH_CLEANUP_FAULT_REASON, KEEP ours' pending_reversal_marker fn+doc (test module at file tail already auto-merged). REGION 2 (407-493): discard ours' inline body, adopt THEIRS verbatim (dispatch_started split -> handle_pre_dispatch_drop / flush_buffered_child_receipts_from_drop / unwind_charge_from_drop -> receipt_metadata). NON-CONFLICTED RIPPLE: (A MANDATORY) line 179 add , None (verified 2-arg). (B) in unwind_charge_from_drop broaden the early-return gate to fire when charge OR payment_authorization present, and reshape to match (&unwind, charge) with ours' 4 arms including pending_reversal_marker on Err+Some and the (Err,None) payment-release warn. (C) in handle_pre_dispatch_drop step 1 broaden `if charge_result().is_some()` to `|| self.payment_authorization.is_some()`. Skipping B/C compiles but reintroduces the P1 MustPrepay leak.
7. STEP 6 - tests/chio_runtime.rs: UNION both new blocks verbatim. Replace the tangled span (working-tree ~1891-4531, all 6 markers): emit OURS full drop_guard_reverse_failure_records_pending_reversal fn from stage :2 WITH its own copy of the spawn scaffold and its own closing }, then a blank line, then THEIRS full RFC-0002 block from stage :3 verbatim (ending at the shared 4531 brace). Do NOT merge the two spawn scaffolds; do NOT thread any budget signature into this file (0 occurrences of the ripple symbols in ours' kept fn).
8. STEP 7 - relay.rs (iroh, non-money): REGION 1 imports - keep ours' common group, put theirs' 3 new symbols (note_router_liveness, run_directory_reloader, DirectoryReloadConfig) into the #[cfg(feature=\"iroh\")] group. REGION 2 - keep theirs' iroh_background abort block but prefix #[cfg(feature=\"iroh\")]; keep ours' cfg-gated mount.shutdown block. NON-CONFLICTED RIPPLE (MANDATORY): prefix the auto-merged, unmarked iroh_background DEFINITION (working-tree ~333-384) with #[cfg(feature=\"iroh\")] - else default build fails on iroh_mount/DirectoryReloadConfig/set_router_alive.
9. STEP 8 - check-chio-transaction-passport.sh: accept OURS (2-line shellcheck comment + source of scripts/lib/chio-proof-trusted-keys.sh). CROSS-FILE RIPPLE (MANDATORY): add THEIRS 6 web3 exports (CONTRACT_PACKAGE_ID, REVIEWED_MANIFEST_HASH, ROOT_REGISTRY/IDENTITY_REGISTRY/ESCROW/BOND_VAULT _RUNTIME_CODEHASH with their ${VAR:-0x...} defaults) into scripts/lib/chio-proof-trusted-keys.sh between ORACLE_KEYS and ALLOWED_CHAIN_IDS. Forgetting this makes chio proof verify fail-closed-DENY the new web3 fixtures.
10. STEP 9 - Resolve the 4 trivial conflicts: chio-control-plane/Cargo.toml (dep dedup), chio-store-sqlite budget_store/tests.rs (union tests), spec/schemas/MANIFEST.sha256 (regenerate after all schema files settle), and confirm evaluation_helpers.rs from Step 1. Then git add all resolved files.
11. STEP 10 - GATE, in order: (a) cargo build --workspace (cold cache, several min); (b) cargo build -p chio-cli (default, no iroh) AND cargo build -p chio-cli --features iroh - BOTH must pass (the iroh build hides default-build breaks); (c) cargo clippy --workspace -- -D warnings (catches leftover unused deny_metadata / arity mismatch); (d) cargo test --workspace; (e) cargo fmt --all -- --check; (f) run scripts/tests/check-chio-transaction-passport.test.sh and the flagship-wall-stops-money.sh demo; (g) grep -rn 'em dash' guard and a final grep for any 2-arg budget_execution_receipt_metadata or ungated iroh symbol; (h) run graphify update . per house rules.
12. STEP 11 - Adversarial self-review before commit: grep the whole kernel crate for `budget_execution_receipt_metadata(` and eyeball every call has 3 args; grep for `mark_runtime_admission_reservations_retained_fail_closed` on all abort/incomplete arms; confirm no `<<<<<<<`/`=======`/`>>>>>>>` remain (git diff --check); confirm release_admitted_capability_budget in the reserving-response helper is gated on budget_lease_acquired.

## Risks (money-path behaviors a wrong merge breaks)

- MONEY R1 (unpaid nonce mint / prepayment not refunded / sibling share leak): if async_evaluation_core.rs region 1 is taken as THEIRS or ours' PreflightHoldDisposition match is dropped, the entire ReserveForCaller MustPrepay path vanishes - execution nonces become mintable without settled prepayment, captured prepayments are not refunded on tear-down, and the delegated sibling share leaks. Guarded by ours' MustPrepay/reserve tests.
- MONEY R2 (frozen prepaid facilitator hold, P1): if kernel_drop_guard.rs edits B/C are skipped (gate left at charge_result().is_some()), a no-ceiling MustPrepay auth (charge==None) is never unwound on abort/drop and the prepaid hold stays open at the facilitator - payer funds frozen. Compiles and passes most tests; caught only by the sim_payment regression tests from commit 67760a581.
- MONEY R3 (unrecoverable burned lease, fail-closed->fail-open regression): if theirs' mark_runtime_admission_reservations_retained_fail_closed wrapper is dropped on ANY cancelled/incomplete/deny/side-effect arm (i.e. resolving those regions as OURS), aborted invocations silently lose the retained_* runtime-reservation ids and operators cannot recover a burned single-use destructive-lease / treaty / swarm continuation from the signed receipt. Applies across async_evaluation_core.rs, nested_flow_evaluation.rs, validation.rs (both incomplete arms), kernel_drop_guard.rs. Guarded by theirs' chio_runtime_admission_* and url_elicitation_monetary_reversal_failure tests.
- MONEY R4 (child-budget oversubscription past sibling-sum cap): if budget_lease_acquired is threaded incorrectly - releasing when not acquired (e.g. leaving build_execution_nonce_authorization_reserving_response's release unconditional, or omitting the field on the ReserveForCaller PreDispatchCleanupDeny) - the refcount underflows and frees a still-held overlapping sibling's share. This is the exact bug theirs' refcount prevents; oversubscription is a real spend-limit breach.
- MONEY R5 (lost pending_reversal audit breadcrumb): if the pending_reversal_marker fold is dropped from kernel_drop_guard.rs unwind_charge_from_drop (Err+Some arm), a failed on-drop reversal leaves the budget hold open with NO durable terminal.disposition='pending_reversal' marker, so the reaper cannot later close it - silent hold leak with no crash. Guarded by ours' drop_guard_reverse_failure_records_pending_reversal test (which must survive the tests/chio_runtime.rs union).
- MONEY R6 (NonceLinkMissing / authoritative-spend receipt rejection): if validation.rs is taken as THEIRS (2-arg, no premint, expression-form arms), authoritative spend receipts lose execution-nonce cross-binding and fail is_authoritative_spend_receipt with NonceLinkMissing; the file also won't compile (undefined merged_extra_metadata).
- COMPILE R7 (arity mismatch under -D warnings): three auto-merged-from-theirs 2-arg budget_execution_receipt_metadata calls (kernel_drop_guard.rs:179, nested_flow_evaluation.rs:807-810, async_evaluation_core.rs:899-902) sit OUTSIDE conflict markers and will not compile against the merged 3-arg def - the classic 'markers gone but build red' trap. Also: keeping ours' inline match in async region 4 alongside theirs' deny_metadata local yields an unused-variable warning (fails -D warnings) plus the 2-arg break.
- BUILD R8 (default binary loses iroh containment / fail-open): if relay.rs region 2 is resolved but the unmarked iroh_background definition (~333-384) is not cfg-gated, or the 3 new imports land in the un-gated group, the DEFAULT (non-iroh) build breaks (unresolved iroh_mount/DirectoryReloadConfig/set_router_alive) and the scoped non-iroh CI leg fails. The --features iroh build hides this - must build BOTH. Conversely taking theirs wholesale silently restores a mandatory iroh dep; taking ours wholesale silently drops the fail-closed directory reloader + liveness watchdog.
- RUNTIME R9 (verifier fail-closed DENY): if the shell script is resolved to ours-only but the 6 web3 vars are NOT ported into scripts/lib/chio-proof-trusted-keys.sh, chio proof verify runs without the trusted contract package id / manifest hash / 4 runtime codehashes and fail-closed-DENIES origin/main's new public-settlement fixtures, breaking check-chio-transaction-passport.sh CI - a false-negative that looks like a verifier regression.
- COVERAGE R10 (lost regression proofs): merging the two tests/chio_runtime.rs spawn scaffolds into one fuses two unrelated async test bodies (compile error or wrong-receipt assertion); forgetting a dedicated closing } for ours' fn swallows theirs' first helper; dropping ours' fn silently removes the only guard on the durable pending_reversal marker.

## Verification

"PROVE OURS SURVIVED (money path): (1) tests/chio_runtime.rs::drop_guard_reverse_failure_records_pending_reversal - asserts a failed on-drop reversal records exactly one cancellation receipt with metadata[budget_authority][terminal][disposition]=='pending_reversal' and a string hold_id (Theme E + R5). (2) The sim_payment / MustPrepay regression suite from commit 67760a581 - proves a no-ceiling MustPrepay abort/drop refunds/releases the prepaid facilitator hold (R2). (3) The ReserveForCaller/MustPrepay prepay+refund tests - prove no unpaid nonce is minted and a denied reservation leaves the payer net-unbilled (R1). (4) is_authoritative_spend_receipt / NonceLinkMissing tests - prove execution-nonce cross-binding survives on authoritative spends and is correctly absent (None) on reversal/incomplete receipts (R6). PROVE THEIRS SURVIVED (safety): (5) drop_non_monetary_post_dispatch_records_cancellation_receipt - asserts Decision::Cancelled reason=='tool evaluation future dropped after admission' (Theme D broadened const). (6) The chio_runtime_admission_* retain/release suite - proves runtime-admission leases are retained on abort and released correctly (Theme B/C). (7) url_elicitation_monetary_reversal_failure_records_monetary_hold_ids_{single,nested_flow} - prove retained_* ids appear in receipts (Theme B, R3). (8) child-capability admission refcount tests - prove no double-free/oversubscription of sibling-sum shares (R4). GATE SEQUENCE: cargo build --workspace; then cargo build -p chio-cli AND cargo build -p chio-cli --features iroh (BOTH - the iroh build masks default breaks, R8); cargo clippy --workspace -- -D warnings (catches unused deny_metadata + arity); cargo test --workspace; cargo fmt --all -- --check; run scripts/tests/check-chio-transaction-passport.test.sh and flagship-wall-stops-money.sh (R9). ADVERSARIAL REVIEW: git diff --check for stray markers; grep the kernel crate for every budget_execution_receipt_metadata( and confirm 3 args at each (special attention to the 3 auto-merged ripple sites); grep every abort/incomplete arm for the mark_...fail_closed wrapper; confirm release_admitted_capability_budget in the reserving-response helper is gated on budget_lease_acquired (not unconditional); grep relay.rs and confirm no un-cfg-gated iroh symbol; confirm the two spawn scaffolds in chio_runtime.rs remain separate and both test fns are independently braced. Because both sides' behaviors are load-bearing money/safety invariants, a green build is necessary but NOT sufficient - R2/R3/R4/R5 all compile-and-mostly-pass while silently regressing; the specific named tests above are the real acceptance bar. Recommend running the code-review / security-review skill on the final staged diff before commit."

## Per-file recommended resolutions

### async_evaluation_core.rs  (confidence: high)
- **Collision:** Four `<<<<<<< / ======= / >>>>>>>` regions, all where the two refactors touch the same expressions. Region 1 (490-578, preflight-required branch): ours replaced the single `build_execution_nonce_preflight_allow_response_after_cleanup(...6 args...)` call with a `match preflight_disposition {...}`; theirs kept the single call but added a 7th arg `budget_lease_acquired`. Same call site, incompatible shapes. Regions 2 (777-796, RequestCancelled arm) and 3 (826-845, RequestIncomplete arm): the SAME metadata expression -- ours = bare `match(...) { budget_execution_receipt_metadata(charge, Some(("reversed",reverse)), None) ... }` (3-arg, no wrapper); theirs = `mark_runtime_admission_reservations_retained_fail_closed( match(...) { budget_execution_receipt_metadata(charge, Some(("reversed",reverse))) ... } )` (2-arg, wrapped). Two orthogonal edits to one expression. Region 4 (915-930, side-effect Err(e) arm): the final argument to `build_deny_response_with_metadata` -- ours = inline 3-arg match; theirs = the precomputed `deny_metadata` local. Compounding collision: the two symbol signatures diverge (`budget_execution_receipt_metadata` ours 3-arg vs theirs 2-arg; `..._preflight_allow_response_after_cleanup` ours 6-arg vs theirs 7-arg; `PreDispatchCleanupDeny` gains a `budget_lease_acquired` field), and the merged helper definitions (validation.rs, evaluation_helpers.rs) will be the union (3-arg receipt-metadata, 7-arg preflight-allow, struct-with-field), so any call left in the other side's arity will not compile.
- **Recommended:** Adopt the union (option 1). Precise merged result per region:

REGION 1 (490-578): Keep OURS' `match preflight_disposition { ReverseForRetry => ..., ReserveForCaller => ... }` verbatim (theirs has no such concept), with three insertions to satisfy the merged signatures: (a) on the `ReverseForRetry` arm, append `budget_lease_acquired,` as the 7th argument to `build_execution_nonce_preflight_allow_response_after_cleanup(request, now, matched_grant_index, cap, &budget_mutation, extra_metadata, budget_lease_acquired)`; (b) in the `ReserveForCaller` arm's `PreDispatchCleanupDeny { ... }` literal (our lines 518-529), add the field `budget_lease_acquired,` (the merged struct requires it); (c) pass `budget_lease_acquired` into `build_execution_nonce_authorization_reserving_response(...)` so its internal non-monetary `release_admitted_capability_budget` is gated on it (requires the corresponding param on the helper in evaluation_helpers.rs -- see risk/non_conflicted note). Drop theirs' single 7-arg call (its behavior is subsumed by the ReverseForRetry arm).

REGIONS 2 (777-796) and 3 (826-845): Union the two orthogonal edits -- keep THEIRS' `mark_runtime_admission_reservations_retained_fail_closed( ... )` wrapper around the match expression, and inside the match keep OURS' 3-arg `self.budget_execution_receipt_metadata(charge, Some(("reversed", reverse)), None)`. Result: `mark_runtime_admission_reservations_retained_fail_closed( match (budget_mutation.charge_result(), unwind.as_ref()) { (Some(charge), Some(reverse)) => self.merge_budget_receipt_metadata(extra_metadata.clone(), self.budget_execution_receipt_metadata(charge, Some(("reversed", reverse)), None)), _ => extra_metadata.clone() } )` passed as the last arg to `build_cancelled_response_with_metadata` / `build_incomplete_response_with_output_and_metadata`.

REGION 4 (915-930): Choose THEIRS' `deny_metadata` for the conflict body (the precomputed local at 895-909 already applies both `merge_budget_receipt_metadata` and the fail-closed wrapper). Drop OURS' inline match (its only deltas were the `None` third arg and the absence of the fail-closed wrapper; the former is folded into the local by the fix below, the latter is exactly theirs' improvement we want).
- **Non-conflicted edits needed:** CRITICAL: the precomputed `deny_metadata` block at lines 895-909 is OUTSIDE the conflict markers (git auto-merged it from theirs), and it contains a 2-arg `self.budget_execution_receipt_metadata(charge, Some(("reversed", reverse)))` at lines 899-902. Against the merged 3-arg definition (ours wins in validation.rs) this will NOT COMPILE. You must edit this non-conflicted region to add the third arg: `self.budget_execution_receipt_metadata(charge, Some(("reversed", reverse)), None)`. This is the load-bearing ripple. Also cross-file (out of this file's scope but required for the whole merge to compile/behave): the merged `evaluation_helpers.rs` must define `build_execution_nonce_preflight_allow_response_after_cleanup` as 7-arg (theirs' `budget_lease_acquired` param + ours' 3-arg internal receipt-metadata call with None), `PreDispatchCleanupDeny` with the `budget_lease_acquired` field, and `build_execution_nonce_authorization_reserving_response` should gain a `budget_lease_acquired` param gating its non-monetary `release_admitted_capability_budget` (ours releases unconditionally today; under theirs' refcount that must be conditional or it double-releases an overlapping sibling's share); `validation.rs` `budget_execution_receipt_metadata` must be the ours 3-arg version. The other three `budget_execution_receipt_metadata` sites in this file (324, 383, 434) and all `PreDispatchCleanupDeny` literals outside region 1 (470-482, 592-602, 612-622, 869-879) are already correct in the working tree (3-arg / field present) and need no edit.
- **Risk:** If regions 2/3/4 are taken as ours (or the fail-closed wrapper is dropped), aborted cancelled/incomplete/side-effect invocations silently lose the `retained_*` runtime-reservation ids -> operators cannot recover a burned destructive-lease / treaty / swarm continuation (the exact regression theirs' commit c201afbd0 fixed). If region 1 is taken as theirs, the entire MustPrepay ReserveForCaller path vanishes: unpaid execution nonces become mintable, captured prepayments are not refunded on tear-down, and the delegated sibling share leaks -- a money-path correctness regression. If the non-conflicted line 899-902 2-arg call is not patched to 3-arg, the crate fails to compile (arity mismatch) with `-D warnings`. If `budget_lease_acquired` is not added to the ReserveForCaller `PreDispatchCleanupDeny` literal, compile error (missing field). If `budget_lease_acquired` is threaded incorrectly (release when not acquired) into the reserving/preflight helpers, the refcount underflows and frees a still-held overlapping sibling's child-budget share -> oversubscription past the sibling-sum cap (the exact bug theirs' refcount prevents). If OURS' inline match is kept in region 4 while theirs' `let deny_metadata` block remains, you get both an unused-variable warning (-D warnings failure) and the 2-arg compile error.

### nested_flow_evaluation.rs  (confidence: high)
- **Collision:** Three conflict regions, all in the tail of the nested-flow monetary invocation error handling, all colliding on the SAME expression: the receipt-metadata argument passed to a build_*_response_with_metadata builder. In each, the collision is between ours (append `None` -> 3-arg `budget_execution_receipt_metadata`) and theirs (wrap the whole `match` in `mark_runtime_admission_reservations_retained_fail_closed(...)` and keep 2-arg). The two edits are ORTHOGONAL (one changes the inner call's arity, the other wraps the outer expression), so they are a false/mechanical conflict, not a semantic contradiction. Regions: (1) lines 686-709 `KernelError::RequestCancelled` arm -> `build_cancelled_response_with_metadata`; (2) lines 735-758 `KernelError::RequestIncomplete` arm -> `build_incomplete_response_with_output_and_metadata`; (3) lines 823-838 the `Err(error)` (tool-side-effect-may-have-run) arm -> `build_deny_response_with_metadata`, where ours re-computes an inline `match` (unwrapped) while theirs passes the pre-computed, already-mark-wrapped `deny_metadata` binding.
- **Recommended:** Adopt the union (option 1). Concretely: (A) The merged `budget_execution_receipt_metadata` signature is the 3-arg one (validation.rs:756, `execution_nonce_id: Option<&str>`); all call sites must be 3-arg. (B) Region 1 (RequestCancelled, ~686-709) resolves to: `self.mark_runtime_admission_reservations_retained_fail_closed( match (budget_mutation.charge_result(), unwind.as_ref()) { (Some(charge), Some(reverse)) => self.merge_budget_receipt_metadata(runtime_admission_metadata.clone(), self.budget_execution_receipt_metadata(charge, Some(("reversed", reverse)), None)), _ => runtime_admission_metadata.clone(), } )` -- i.e. theirs' wrapper + ours' trailing `None`. (C) Region 2 (RequestIncomplete, ~735-758) resolves identically (wrapper around the match, `None` appended to the inner call). (D) Region 3 (Err(error) deny, ~823-838) resolves by taking THEIRS: pass the already-computed `deny_metadata` binding (which is already mark-wrapped by the non-conflicted line 814), discarding ours' redundant inline match. Type-checks because `mark_runtime_admission_reservations_retained_fail_closed` and `merge_budget_receipt_metadata` both return `Option<serde_json::Value>`, `runtime_admission_metadata` is `Option<serde_json::Value>`, and the builders already accept `Option<serde_json::Value>` in ours, so adding the wrapper is transparent to the callee signatures. This preserves ours' nonce-binding placeholder (None on all reversal paths) and theirs' fail-closed reservation retention plus the `budget_lease_acquired` refcount plumbing simultaneously.
- **Non-conflicted edits needed:** One mandatory edit OUTSIDE the conflict markers: in the `Err(error)` arm's hoisted `let deny_metadata = match (...) {...}` block, the call at lines 807-810 is `self.budget_execution_receipt_metadata(charge, Some(("reversed", reverse)),)` -- only 2-arg. It arrived wholesale from theirs (this pre-computation block never existed on ours, so git placed it outside the markers), and it will NOT compile against the merged 3-arg signature. Fix: append `None,` so it reads `self.budget_execution_receipt_metadata(charge, Some(("reversed", reverse)), None)`. All other call sites in the file (lines 249, 302, 346) already carry the 3-arg `None` from the clean auto-merge and need no change. No further ripple: `budget_lease_acquired` (line 374 def; used at 393/409/433/453/488/503/596/786) and the `PreDispatchCleanupDeny` branch are theirs-only and non-conflicted, already consistent.
- **Risk:** If region 3's non-conflicted 2-arg call at lines 807-810 is not patched, the crate fails to compile (E0061 wrong number of arguments) even though all three marker regions look resolved -- this is the classic "conflict markers gone but build red" trap. If instead one takes theirs verbatim in the marker regions (dropping `None`), same compile failure at every reversal call. If one takes ours verbatim (dropping the `mark_runtime_admission_reservations_retained_fail_closed` wrapper), it compiles but silently regresses origin/main's money/cleanup safety fix: aborted cancelled/incomplete/deny paths would no longer emit `retained_*` reservation ids, so an operator cannot recover a burned destructive-lease / treaty / swarm continuation from the signed receipt -- a fail-closed-to-fail-open regression on the authorization money path. Getting the deny arm wrong (using ours' inline unwrapped match instead of the mark-wrapped `deny_metadata`) would also drop retention marking on that one arm only, an easy-to-miss asymmetry.

### kernel_drop_guard.rs  (confidence: high)
- **Collision:** Two marker regions collide. Region 1 (lines 12-36): both sides edited the header - ours added the `pending_reversal_marker` fn plus `POST_ADMISSION_DROP_REASON = "...after monetary admission"`; theirs kept `POST_ADMISSION_DROP_REASON = "...after admission"` and added `PRE_DISPATCH_CLEANUP_FAULT_REASON`. Region 2 (lines 407-493): both rewrote the entire `Drop::drop` body between the shared `if !self.armed { return; }` head and the shared tail (`scope_pre_invocation_guard_evidence` + `build_cancelled_response_with_metadata`). The tail auto-merged to THEIRS and expects a local named `receipt_metadata`; our body produced `extra_metadata` and references `self.charge_result`, a field THEIRS deleted (renamed to `budget_mutation`), so our body cannot compile against the merged struct. The whole middle of the file (lines 37-399: the theirs struct, 8-arg `new`, `unwind_charge_from_drop`, `handle_pre_dispatch_drop`, helpers) auto-merged to theirs because our side left it identical to base. LATENT (outside the markers) collision: the merged `budget_execution_receipt_metadata` definition is our 3-arg version (validation.rs is separately conflicted and resolves to 3-arg; production callers already 3-arg), but theirs' auto-merged `unwind_charge_from_drop` at line 179 still calls it with 2 args -> hard compile error even under a naive all-theirs resolution. Both production callers (async_evaluation_core.rs:628, nested_flow_evaluation.rs:492) already use the theirs 8-arg `new(&budget_mutation, ..., budget_lease_acquired)`, so the struct/new must be theirs.
- **Recommended:** Take option 2. REGION 1 (12-36): union - keep our `pending_reversal_marker` fn and its doc comment; keep BOTH consts; for `POST_ADMISSION_DROP_REASON` adopt theirs' text "tool evaluation future dropped after admission" (the guard now fires for non-monetary admissions) and keep theirs' `PRE_DISPATCH_CLEANUP_FAULT_REASON`. Our test module at the file tail is already auto-merged in and stays. REGION 2 (407-493): discard our inline body entirely and adopt THEIRS verbatim (the `if !self.dispatch_started { self.handle_pre_dispatch_drop(); return; }` branch, `flush_buffered_child_receipts_from_drop()`, `let reversed_metadata = self.unwind_charge_from_drop();`, `let receipt_metadata = self.kernel.mark_runtime_admission_reservations_retained_fail_closed(reversed_metadata);`), which produces the `receipt_metadata` local the shared tail consumes. Our region-2 logic does not vanish - it migrates into theirs' helpers via the non-conflicted edits below. Net: theirs' struct (budget_mutation + budget_lease_acquired + dispatch_started + child_receipts), 8-arg `new`, and pre/post-dispatch split are the merged shape; our money semantics and 3-arg signature are folded into the helpers.
- **Non-conflicted edits needed:** Three edits OUTSIDE the conflict markers are required. (A) COMPILE-BREAKING, mandatory: line 179 inside theirs' `unwind_charge_from_drop` calls `budget_execution_receipt_metadata(charge, Some(("reversed", reverse)))` with 2 args, but the merged definition (our cross-binding 3-arg version, validation.rs) takes `execution_nonce_id: Option<&str>` - change to `budget_execution_receipt_metadata(charge, Some(("reversed", reverse)), None)`. Without this the file does not compile even under a pure-theirs resolution. (B) BEHAVIOR-PRESERVING, required to keep our P1 fix: in `unwind_charge_from_drop` replace the charge-only early return at line 166 (`let Some(charge) = self.budget_mutation.charge_result() else { return base; };`) with a gate that fires when charge OR payment_authorization is present (`if charge.is_none() && self.payment_authorization.is_none() { return base; }`), then reshape the match to `match (&unwind, charge)` with our four arms: Ok(Some)+Some folds the 3-arg reversed metadata, Err+Some records `pending_reversal_marker(&charge.budget_hold_id, &error.to_string())` under `budget_authority`, Err+None warns "failed to release dropped post-admission payment authorization" and returns base. (C) same broadening in `handle_pre_dispatch_drop` step 1 at line 206: change `if self.budget_mutation.charge_result().is_some() {` to `if self.budget_mutation.charge_result().is_some() || self.payment_authorization.is_some() {` so a pre-dispatch drop of a no-charge MustPrepay still calls `unwind_aborted_monetary_invocation` (whose dispatch.rs body, our fix, already releases/refunds the auth when charge is None). If B/C are skipped the merge compiles but reintroduces the leaked prepaid MustPrepay hold that commit 67760a581 fixed.
- **Risk:** If region 2 is naively resolved to "ours", the body references `self.charge_result` (deleted) and yields `extra_metadata` not the `receipt_metadata` the merged tail expects: hard compile failure. If resolved to "theirs" without edit A, line 179's 2-arg call fails against the merged 3-arg signature: hard compile failure. If resolved to "theirs" and edit A is applied but edits B/C are skipped, it compiles and passes most tests but silently reintroduces a P1 money defect - a no-ceiling MustPrepay abort/drop leaves the prepaid hold open at the facilitator (payer funds frozen) because the monetary unwind is gated on charge_result().is_some(); our sim_payment regression tests from 67760a581 would catch it. Getting the `pending_reversal_marker` fold wrong loses the durable audit breadcrumb for stuck holds (operator-recoverability gap) without a functional crash. Lease/child-receipt/fail-closed behavior from theirs must be left untouched or F02/F08 audit and sibling-sum overlap protections regress.

### validation.rs  (confidence: high)
- **Collision:** Both sides rewrote the same statement: the final metadata argument of the single `self.build_incomplete_response_with_output_and_metadata(request, Some(tool_call_output), &reason, timestamp, Some(charge.grant_index), <ARG>)` call in the reconcile finalize fn. This is the sole conflict hunk (lines 1488-1502; verified exactly one `<<<<<<<`). Ours passes the bare local `merged`; theirs passes `self.mark_runtime_admission_reservations_retained_fail_closed(merged_extra_metadata)`. The collision is compounded by a variable-name divergence: git auto-merged the surrounding context to OUR block-form arm where the local is named `merged`, but theirs' conflict text references `merged_extra_metadata`, which does not exist in the merged context. The other differences (3-arg vs 2-arg `budget_execution_receipt_metadata`, the premint block, the block-vs-expression arm form, the extra `preminted_execution_nonce` allow-builder arg) were auto-resolved in OUR favor outside the markers, so they are not part of the hunk but frame the correct fix.
- **Recommended:** Adopt Option 1. Replace the conflict hunk (lines 1488-1502, i.e. everything from `<<<<<<< HEAD` through `>>>>>>> origin/main`) so the Incomplete arm reads exactly:\n\n                self.build_incomplete_response_with_output_and_metadata(\n                    request,\n                    Some(tool_call_output),\n                    &reason,\n                    timestamp,\n                    Some(charge.grant_index),\n                    // The tool ran (a side effect may have committed) but the\n                    // stream ended incomplete, so any runtime-admission lease\n                    // consumed at admission is retained, not released. Mark it\n                    // so the burned lease is recoverable from the receipt,\n                    // matching the RequestIncomplete error arm.\n                    self.mark_runtime_admission_reservations_retained_fail_closed(merged),\n                )\n            }\n\nConcretely: keep ours' block-form arm and its local `merged` (built at lines 1478-1481 from merge_metadata_objects(financial_json, merge_budget_receipt_metadata(extra_metadata, budget_metadata)) with budget_metadata's 3rd arg = None); wrap that `merged` with theirs' helper; use ours' closing `)` then `}` (block form), NOT theirs' `),`/`),`; rename theirs' `merged_extra_metadata` -> `merged`; retain theirs' 5-line comment. Keep the merged 3-arg `budget_execution_receipt_metadata` signature (line 756) and ours' premint block untouched. Type-checks: `merged` is Option<serde_json::Value>; mark_runtime_admission_reservations_retained_fail_closed(Option<Value>) -> Option<Value> (dispatch.rs:431); build_incomplete_response_with_output_and_metadata's last param is Option<serde_json::Value> (terminal_responses.rs:63). Leave the Value/Complete arm unwrapped, matching theirs.
- **Non-conflicted edits needed:** One same-file spot warrants an edit that git did NOT flag (it is outside the markers): the SECOND incomplete-stream finalize arm at merged line 1091, inside ours-only fn (the provisional/no-realized-cost reversal path, commit 9efcfe629 - uses `("reversed", &reverse)`, builds its own local `merged` at lines ~1073-1077). That arm calls build_incomplete_response_with_output_and_metadata(..., merged) unwrapped. Theirs' commit 84e98b9d0 explicitly intended to wrap BOTH incomplete-stream finalize branches; this second branch did not exist on theirs' side so no conflict arose, but to fully honor theirs' fail-closed retain-mark policy it should also become `self.mark_runtime_admission_reservations_retained_fail_closed(merged)`. This is a behavior-completeness edit, not a compile requirement (it compiles either way; helper is fail-closed and safe). Recommend making it for consistency. Ripple check otherwise clean: all four `budget_execution_receipt_metadata` callers in the merged file (lines 1073, 1451, 1473, plus def 756) already use the 3-arg form, and build_allow_response_with_metadata in the merged tree already carries the `preminted_nonce` param - no arity breakage elsewhere in this file.
- **Risk:** If resolved by naive "take theirs," the file will not compile (undefined `merged_extra_metadata`) and would also strip ours' nonce cross-binding, causing authoritative spend receipts to fail is_authoritative_spend_receipt with NonceLinkMissing. If resolved by naive "take ours," it compiles but silently reintroduces the bug theirs fixed: an incomplete-stream (or stream-limit-truncated) tool output that consumed a runtime-admission lease finalizes with the lease unmarked, so the burned destructive-lease / treaty-continuation / swarm-continuation id is not recoverable from the signed receipt (operators cannot re-issue it) - a real money/authorization-path regression. If the closing punctuation is copied from theirs (expression form `),`/`),`) into ours' block-form arm, it will be a brace/paren mismatch and fail to compile. If the line-1091 reversal-path arm is left unwrapped, the retain-mark gap persists for the no-realized-cost provisional path only.

### chio_runtime.rs  (confidence: high)
- **Collision:** There is NO semantic collision. This is a textual adjacency conflict: both sides inserted brand-new code at the same insertion point (immediately after chio_runtime_live_parent_and_vendor_calls_expose_package_valid_receipts, base line ~1889). git's diff3 then coincidentally aligned two shared lines that appear identically in BOTH new blocks - the spawn scaffold `let kernel = std::sync::Arc::new(kernel); let eval = { let kernel = Arc::clone(&kernel); tokio::spawn(async move { kernel.evaluate_tool_call(&request).await }) };` (working-tree lines 2515-2521) and the trailing `}` (line 4531) - splitting one logical union into two interleaved conflict hunks. Region 1 (working-tree 1891-2514): OURS = 1892-1920 = the setup half of our test; THEIRS = 1922-2513 = the RFC-0002 helpers + the setup half of drop_non_monetary_post_dispatch_records_cancellation_receipt. Common 2515-2521 = the shared spawn scaffold. Region 2 (2522-4530): OURS = 2523-2547 = the assertion half of our test; THEIRS = 2549-4529 = the assertion half of theirs' first test plus the entire rest of theirs' suite. Symbol sets are DISJOINT: our fn drop_guard_reverse_failure_records_pending_reversal does not exist in theirs (grep=0); theirs' helpers make_fabricated_drop_charge/ParkingServer do not exist in ours (grep=0). No function/struct/impl name is defined by both sides.
- **Recommended:** Adopt the UNION (option 1). Replace the entire tangled span (working-tree lines 1891 through 4531 inclusive, i.e. all six markers and everything between them) with, in order: (1) OUR full test function copied verbatim from stage :2 - the `#[tokio::test(flavor = "multi_thread", worker_threads = 2)] async fn drop_guard_reverse_failure_records_pending_reversal() { ... }` including its OWN copy of the `let kernel = Arc::new(kernel); ... tokio::spawn(...)` scaffold and its OWN closing `}`; (2) a blank line; (3) THEIRS full block copied verbatim from stage :3, starting at `// --- RFC-0002 drop-guard unwind tests ---` and running through the final test url_elicitation_monetary_reversal_failure_records_monetary_hold_ids_nested_flow and its closing `}` (this is the existing line-4531 brace). Equivalently: `git show :2:...chio_runtime.rs` supplies the whole ours tail (its lines ~1677-1738) and `git show :3:...chio_runtime.rs` supplies the whole theirs tail; concatenate the two tails. No signature adaptation is needed INSIDE this file: our function only calls helpers that survive the merge (make_monetary_config and make_monetary_grant are defined by theirs' merged support code; ReverseFailingBudgetStore and PendingMonetaryServer are defined in the auto-merged sibling tests/support_monetary.rs, which I confirmed already contains BOTH in the working tree), and our bare `Duration::from_secs(1)` resolves because `use std::time::Duration;` is brought into the shared include! module scope by tests/emergency.rs. Do NOT collapse the two spawn scaffolds into one and do NOT try to thread budget_lease_acquired / the 3-arg budget_execution_receipt_metadata into this file - none of those symbols appear in our kept function, and theirs' occurrences stay internal to theirs' verbatim block.
- **Non-conflicted edits needed:** None required in this file. I audited every ripple symbol from the merge brief: budget_execution_receipt_metadata = 0 occurrences in all three stages of this file (no 2-arg-vs-3-arg call to fix here); budget_lease_acquired = 0 occurrences here; mark_runtime_admission_reservations_retained_fail_closed appears only at working-tree lines 4319/4335/4350, all INSIDE theirs' verbatim-kept block (consistent with theirs' production API), so no cross-boundary arity break; mark_dispatch_started at line 1625 is in the pre-conflict COMMON region and is unchanged by the resolution. Because the union keeps our function and theirs' block each fully self-contained, there is no non-conflicted call site in this file that breaks against a merged signature. (Out-of-file, not part of this task but load-bearing for these tests to run: verify the auto-merged tests/support_monetary.rs retained our ReverseFailingBudgetStore, and that the production drop-guard merge in kernel_drop_guard.rs/dispatch.rs kept our pending_reversal escalation.)
- **Risk:** If resolved wrong: (a) Dropping our test (option 3 or forgetting option 2's re-add) silently removes the only guard on the durable pending_reversal terminal marker - a future refactor could reintroduce a leaked-hold regression undetected. (b) Merging the two spawn scaffolds into a single shared block (naively trusting git's alignment) fuses two unrelated async test bodies into one, producing a function that references two different `request`/receipt-log setups and fails to compile or asserts against the wrong receipt. (c) Forgetting to add a dedicated closing `}` for our function (because git shows the 4531 brace as shared) leaves our fn body unterminated and swallows theirs' first helper - a compile error. (d) Attempting to "modernize" our test by rewriting its assertions against theirs' Decision::Cancelled shape risks changing what money-path invariant is checked. The safe path is verbatim concatenation of the two stage tails. Residual runtime (not compile) risk lives OUTSIDE this file: our test will fail at runtime if the production-code merge did not preserve the pending_reversal escalation, and won't compile if support_monetary.rs lost ReverseFailingBudgetStore - both confirmed present in the current working tree.

### relay.rs  (confidence: high)
- **Collision:** Two marked regions plus one silently mis-merged region. Region 1 (markers at lines 2-19, the `use super::{...}` block): both sides rewrote the same import list. Ours restructured it into common + `#[cfg(feature="iroh")]` groups; theirs kept it flat but added note_router_liveness, run_directory_reloader, DirectoryReloadConfig. The overlap is the exact same set of imported symbols with divergent structure (grouped vs flat) and divergent membership (theirs has 3 extra). Region 2 (markers at lines 390-399, immediately after `let serve_result = service.serve(listener).await...`): ours is a bare `#[cfg(feature="iroh")]` (attaching to the mount-shutdown block that follows), theirs is the `if let Some((reloader, watchdog)) = iroh_background { ... }` abort block. Same source region, mutually exclusive text. THIRD, hidden collision: git auto-merged theirs' `iroh_background` DEFINITION block (working-tree lines 333-384) WITHOUT conflict markers and WITHOUT any cfg gate, even though it references the cfg-gated local `iroh_mount` (defined `#[cfg(feature="iroh")]` at line 301) and iroh-only symbols DirectoryReloadConfig/run_directory_reloader/note_router_liveness/chio_federation_transport_iroh::metrics::set_router_alive. That block is a real semantic conflict that the textual 3-way merge did not flag.
- **Recommended:** Adopt Option 1, mirroring the already-resolved parent `pheromone.rs` (lines 56-61 there gate note_router_liveness/run_directory_reloader/DirectoryReloadConfig under `#[cfg(feature=\"iroh\")]`). Concretely: REGION 1 - replace the whole marked block with ours' common group unchanged, followed by `#[cfg(feature = \"iroh\")]\nuse super::{\n    build_iroh_outbound_endpoint, build_iroh_router, iroh_transport_metrics_prometheus,\n    load_iroh_serve_inputs, note_router_liveness, run_directory_reloader, DirectoryReloadConfig,\n    IrohServeInputs,\n};` (i.e. take ours' iroh group and add the 3 new theirs symbols to it; unix_now_ms stays in the common group and is used by the reloader). REGION 2 - replace the marked block so it reads: after serve_result, `#[cfg(feature = \"iroh\")]\nif let Some((reloader, watchdog)) = iroh_background {\n    if let Some(reloader) = reloader {\n        reloader.abort();\n    }\n    watchdog.abort();\n}` then keep ours' `#[cfg(feature = \"iroh\")]` on `if let Some(mount) = iroh_mount { let _ = mount.router.shutdown().await; }`, then `serve_result`. This threads no new field and changes no signature (deliver_http_tick and budget_execution_receipt_metadata arities are unrelated to this file); it only unions imports, adopts theirs' abort block, and re-applies ours' cfg gates. Result compiles under `--features iroh` (all symbols resolve, iroh_background is Some path) and under default (iroh_background/reloader/watchdog code is compiled out, iroh_transport_directory still consumed by the not(iroh) block at 196-207 so no unused-var warning).
- **Non-conflicted edits needed:** CRITICAL: one edit is required OUTSIDE the conflict markers. The `iroh_background` definition at working-tree lines 333-384 (`let iroh_background = iroh_mount.as_ref().map(|mount| { ... set_router_alive ... run_directory_reloader ... note_router_liveness ... });`) was auto-merged from theirs with NO cfg gate. It must be prefixed with `#[cfg(feature = \"iroh\")]` (attach the attribute to the `let iroh_background` statement; the explanatory comment at 333-335 can stay above it). Without this, a default/non-iroh build fails: `iroh_mount` is only defined `#[cfg(feature=\"iroh\")]` (line 301), and DirectoryReloadConfig/run_directory_reloader/note_router_liveness/chio_federation_transport_iroh::metrics::set_router_alive are all iroh-only. No other in-file ripple: no 2-arg-vs-3-arg call sites here, and the fn signature (iroh_transport_directory etc.) is unchanged and already consumed by the existing `#[cfg(not(feature=\"iroh\"))]` block at lines 196-207.
- **Risk:** If Region 2's abort block is taken but the non-conflicted iroh_background definition (333-384) is left un-gated, or the 3 new imports are placed in the common (un-gated) group: the DEFAULT (non-iroh) workspace build breaks with unresolved-name / undefined-variable / missing-optional-dependency errors, and the scoped non-iroh CI leg added in 50ab32a6e fails - a fail-open regression against ours' containment goal. If instead theirs is taken wholesale, the default binary silently regains a hard iroh dependency. If ours is taken wholesale, the fail-closed directory reloader and liveness watchdog are silently dropped, a security-behavior regression (no deny-all on bundle expiry / endpoint rotation, no frozen-accept-loop detection). The iroh-enabled build tends to hide the first class of mistake because everything compiles there, so verify with BOTH `cargo build -p chio-cli` (default) and `cargo build -p chio-cli --features iroh`.

### check-chio-transaction-passport.sh  (confidence: high)
- **Collision:** Both sides rewrote the SAME physical region: the inline `export CHIO_...` trusted-key block (base lines ~124-160). Git can't reconcile because ours DELETED that block entirely (moved to the sourced lib) while theirs EDITED it in place (added 6 web3 vars). The single conflict hunk is working-tree lines 123-161: stage :2 (HEAD) = the 2-line `source` of scripts/lib/chio-proof-trusted-keys.sh; stage :3 (origin/main) = the full ~35-line inline block that now contains the 6 new CHIO_PUBLIC_SETTLEMENT_TRUSTED_* web3 vars. The overlapping symbols are the CHIO_PUBLIC_SETTLEMENT_TRUSTED_* / CHIO_*_TRUSTED_*_KEYS exports; ours relocated all of them to the lib, theirs added six that the lib snapshot (taken before origin/main) does not contain. There is no function-signature/arity overlap in this bash file - it is a text region collision, and the real "missing symbols" ripple lands in the separate lib file, not here.
- **Recommended:** Resolve the hunk by ACCEPTING OURS: replace the entire conflict block (working-tree lines 123-161, i.e. `<<<<<<< HEAD` through `>>>>>>> origin/main`) with exactly the two ours lines:
  # shellcheck source=scripts/lib/chio-proof-trusted-keys.sh
  source "$ROOT/scripts/lib/chio-proof-trusted-keys.sh"
Then, in the SEPARATE file scripts/lib/chio-proof-trusted-keys.sh, add theirs 6 new exports between line 27 (`export CHIO_PUBLIC_SETTLEMENT_TRUSTED_ORACLE_KEYS=...`) and line 28 (`export CHIO_PUBLIC_SETTLEMENT_ALLOWED_CHAIN_IDS=...`), verbatim from origin/main:
  export CHIO_PUBLIC_SETTLEMENT_TRUSTED_CONTRACT_PACKAGE_ID="${CHIO_PUBLIC_SETTLEMENT_TRUSTED_CONTRACT_PACKAGE_ID:-chio.official-web3-contracts}"
  export CHIO_PUBLIC_SETTLEMENT_TRUSTED_REVIEWED_MANIFEST_HASH="${CHIO_PUBLIC_SETTLEMENT_TRUSTED_REVIEWED_MANIFEST_HASH:-0x454a9a92b54a835a2776750196b171501bff6e5c02df1a192616194fc0a095cc}"
  export CHIO_PUBLIC_SETTLEMENT_TRUSTED_ROOT_REGISTRY_RUNTIME_CODEHASH="${CHIO_PUBLIC_SETTLEMENT_TRUSTED_ROOT_REGISTRY_RUNTIME_CODEHASH:-0xfc5d76d87b02096c6ae32ce644a2b98ca0bdf3c56700ad16731fad2062e6bd7f}"
  export CHIO_PUBLIC_SETTLEMENT_TRUSTED_IDENTITY_REGISTRY_RUNTIME_CODEHASH="${CHIO_PUBLIC_SETTLEMENT_TRUSTED_IDENTITY_REGISTRY_RUNTIME_CODEHASH:-0xd4f87cc63c00d0640c8f232c8fac5e5cb99bc6cf185ef912225e07fa438614cc}"
  export CHIO_PUBLIC_SETTLEMENT_TRUSTED_ESCROW_RUNTIME_CODEHASH="${CHIO_PUBLIC_SETTLEMENT_TRUSTED_ESCROW_RUNTIME_CODEHASH:-0x03d8f545c330922a33db6473430c50eafd527e04474f31abee2dc1f8c6ab2d36}"
  export CHIO_PUBLIC_SETTLEMENT_TRUSTED_BOND_VAULT_RUNTIME_CODEHASH="${CHIO_PUBLIC_SETTLEMENT_TRUSTED_BOND_VAULT_RUNTIME_CODEHASH:-0x17f7936469584b38404765ac44bd7e2384337983e4bc6448a3500d0637711f09}"
This preserves ours refactor (one sourced lib), preserves theirs new gating vars (now exported to the verifier via the source), keeps flagship-wall-stops-money.sh in sync for free, and satisfies scripts/tests/check-chio-transaction-passport.test.sh (which accepts the vars in either the script or the lib). No new fields to thread; nothing to wrap. This file is read-only for this task - the above is the prescription, not applied.
- **Non-conflicted edits needed:** Inside check-chio-transaction-passport.sh itself: NONE. The line immediately after the conflict, `PUBLIC_SETTLEMENT_REORGED_INDEPENDENT_CHAIN_HEAD_JSON=...` (line 162), is identical on both sides and stays; nothing downstream references the 6 web3 var names by literal in the script body (grep confirms they appear ONLY inside the conflict block), so there is no stale call site to patch and no arity mismatch. The mandatory edit is in a DIFFERENT file: scripts/lib/chio-proof-trusted-keys.sh must gain theirs 6 web3 exports (see recommended_resolution) - otherwise sourcing yields a lib missing those keys. Optional: no change needed to scripts/demo/flagship-wall-stops-money.sh or scripts/tests/check-chio-transaction-passport.test.sh; both keep working once the lib is updated.
- **Risk:** If you resolve to ours-only and FORGET to port the 6 web3 vars into scripts/lib/chio-proof-trusted-keys.sh, the merged verifier gate runs without CHIO_PUBLIC_SETTLEMENT_TRUSTED_CONTRACT_PACKAGE_ID / REVIEWED_MANIFEST_HASH / the four *_RUNTIME_CODEHASH values. Because Chio is fail-closed, `chio proof verify` will DENY the new public-settlement/web3-contract fixtures that origin/main added (261274f86 / f21a58c80), so `check-chio-transaction-passport.sh` (non-schema-only mode) exits non-zero and the CI gate breaks - a false-negative that looks like a verifier regression. Conversely, if you resolve to theirs-only (drop the source line) the script re-inlines a second copy of the trusted keys while flagship-wall-stops-money.sh still sources the lib, producing two divergent key sets that will silently disagree later. Taking ours WITHOUT the lib edit is the dangerous, compiles-but-fails-at-runtime outcome.
